---
phase: 09-restore-clobbered-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sweep/index.ts
  - src/batch/index.ts
  - test/unit/sweep/sweep.test.ts
  - test/integration/sweep.test.ts
  - test/unit/batch/engine.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "ReviewSweeper class is importable from src/sweep/index.ts"
    - "BatchEngine class is importable from src/batch/index.ts"
    - "Sweep tests pass after restoration"
    - "Batch engine tests pass after restoration"
  artifacts:
    - path: "src/sweep/index.ts"
      provides: "ReviewSweeper — periodic review folder cleanup"
      min_lines: 250
      exports: ["ReviewSweeper"]
    - path: "src/batch/index.ts"
      provides: "BatchEngine — retroactive rule application with dry-run"
      min_lines: 380
      exports: ["BatchEngine"]
    - path: "test/unit/sweep/sweep.test.ts"
      provides: "ReviewSweeper unit tests"
      min_lines: 500
    - path: "test/integration/sweep.test.ts"
      provides: "Sweep integration tests"
      min_lines: 120
    - path: "test/unit/batch/engine.test.ts"
      provides: "BatchEngine unit tests"
      min_lines: 700
  key_links:
    - from: "src/sweep/index.ts"
      to: "src/imap/client.ts"
      via: "uses ImapClient for folder operations"
      pattern: "ImapClient"
    - from: "src/batch/index.ts"
      to: "src/sweep/index.ts"
      via: "imports ReviewSweeper for sweep helpers"
      pattern: "sweep"
---

<objective>
Restore the sweep engine (ReviewSweeper) and batch engine (BatchEngine) modules along with their test files.

Purpose: These are the core processing modules that the web routes, server wiring, and main entry point depend on. They must exist before backend wiring can be restored.

Output: Both engine modules restored from pre-clobber git history, adapted if needed for Phase 7/8 type changes, with all tests passing.
</objective>

<execution_context>
@/Users/mike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-restore-clobbered-features/09-RESEARCH.md

Source of truth: `git show f453be7^:<path>` for each deleted file.

NOTE: This plan restores DELETED files only (no merge conflicts with Phase 8). However, the restored files import from imap, rules, actions, log, and config modules. Plan 01 restores the foundation types these modules import. If Plan 01 hasn't completed yet, build may fail on missing types — but the restored source code itself is correct. Plan 01 and 02 are Wave 1 (parallel), so the executor should attempt both and the build check validates after both complete.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore ReviewSweeper and BatchEngine source modules</name>
  <files>src/sweep/index.ts, src/batch/index.ts</files>
  <read_first>
    - git show f453be7^:src/sweep/index.ts (pre-clobber — 272 lines, entire module to restore)
    - git show f453be7^:src/batch/index.ts (pre-clobber — 398 lines, entire module to restore)
    - src/config/schema.ts (current HEAD — verify emailMatchSchema shape for compatibility)
    - src/actions/index.ts (current HEAD — verify executeAction signature hasn't changed)
    - src/rules/evaluator.ts (current HEAD — verify evaluateRules signature)
  </read_first>
  <action>
**src/sweep/index.ts** — Create directories and restore ENTIRE file from `git show f453be7^:src/sweep/index.ts`.

Before writing, verify compatibility:
- The pre-clobber sweep imports `evaluateRules` from `../rules/evaluator.js` — confirm this export still exists in current HEAD.
- The pre-clobber sweep imports `executeAction` from `../actions/index.js` — confirm this export still exists.
- The pre-clobber sweep imports `reviewMessageToEmailMessage` from `../imap/index.js` — this is in the current barrel.
- The pre-clobber sweep uses `ReviewMessage` type — Plan 01 restores the envelope fields on it.

Create `src/sweep/` directory if it doesn't exist, then write the file.

**src/batch/index.ts** — Restore ENTIRE file from `git show f453be7^:src/batch/index.ts`.

Same compatibility checks apply. The BatchEngine imports from sweep, imap, rules, actions, log, and config modules.

Create `src/batch/` directory if it doesn't exist, then write the file.

If either file imports `saveConfig` from config (the pre-clobber index.ts used it), verify it's still exported. If not, adapt the import.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/sweep/index.ts exists and contains `export class ReviewSweeper`
    - src/sweep/index.ts line count >= 250
    - src/batch/index.ts exists and contains `export class BatchEngine`
    - src/batch/index.ts line count >= 380
    - npm run build exits 0 (may require Plan 01 to also be complete)
  </acceptance_criteria>
  <done>ReviewSweeper and BatchEngine modules restored from git history and compatible with current codebase types.</done>
</task>

<task type="auto">
  <name>Task 2: Restore sweep and batch test files</name>
  <files>test/unit/sweep/sweep.test.ts, test/integration/sweep.test.ts, test/unit/batch/engine.test.ts</files>
  <read_first>
    - git show f453be7^:test/unit/sweep/sweep.test.ts (pre-clobber — 559 lines)
    - git show f453be7^:test/integration/sweep.test.ts (pre-clobber — 130 lines)
    - git show f453be7^:test/unit/batch/engine.test.ts (pre-clobber — 740 lines)
    - src/imap/messages.ts (current HEAD after Task 1 or Plan 01 — verify parseMessage signature)
  </read_first>
  <action>
Create necessary test directories and restore all three test files from `git show f453be7^:<path>`:

1. `mkdir -p test/unit/sweep test/unit/batch test/integration`
2. Restore `test/unit/sweep/sweep.test.ts` from pre-clobber.
3. Restore `test/integration/sweep.test.ts` from pre-clobber.
4. Restore `test/unit/batch/engine.test.ts` from pre-clobber.

After restoring, check for compatibility issues:
- If tests import `parseMessage` and call it with 1 arg, that's fine (the `envelopeHeader` param is optional).
- If tests create mock `ReviewMessage` objects, they don't need `envelopeRecipient`/`visibility` fields (optional).
- If tests reference `ImapFetchResult`, the `headers` field is optional so existing mocks are fine.

Fix any imports that don't match current barrel exports (unlikely but check).
  </action>
  <verify>
    <automated>npx vitest run test/unit/sweep/sweep.test.ts test/integration/sweep.test.ts test/unit/batch/engine.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/sweep/sweep.test.ts exists and line count >= 500
    - test/integration/sweep.test.ts exists and line count >= 120
    - test/unit/batch/engine.test.ts exists and line count >= 700
    - All three test files pass (exit 0 from vitest run)
  </acceptance_criteria>
  <done>All sweep and batch test files restored and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Restoration of existing tested code — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | — | — | accept | Pure restoration — pre-existing security posture unchanged |
</threat_model>

<verification>
- `ls src/sweep/index.ts src/batch/index.ts` — both exist
- `npx vitest run test/unit/sweep/ test/integration/sweep.test.ts test/unit/batch/ --reporter=verbose` — all pass
- `npm run build` passes
</verification>

<success_criteria>
ReviewSweeper and BatchEngine modules fully restored with all unit and integration tests passing. Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-clobbered-features/09-02-SUMMARY.md`
</output>
