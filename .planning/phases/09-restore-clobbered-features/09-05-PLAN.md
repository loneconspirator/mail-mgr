---
phase: 09-restore-clobbered-features
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "npm run build exits 0"
    - "npm test passes with all restored + existing tests (22 test files, ~2500+ tests)"
    - "All 10 previously deleted source modules exist and export their expected classes/functions"
    - "All 8 previously deleted test files exist and pass"
    - "All 11 previously degraded files have restored content alongside Phase 8 additions"
  artifacts: []
  key_links: []
---

<objective>
Final integration verification: run full build and full test suite, verify all 29 affected files are restored, and do a visual spot-check of the web UI.

Purpose: Catch any cross-plan integration issues, type mismatches, or missed restorations before closing the phase.

Output: Green build, green tests, verified UI.
</objective>

<execution_context>
@/Users/mike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-restore-clobbered-features/09-RESEARCH.md
@.planning/phases/09-restore-clobbered-features/09-01-SUMMARY.md
@.planning/phases/09-restore-clobbered-features/09-02-SUMMARY.md
@.planning/phases/09-restore-clobbered-features/09-03-SUMMARY.md
@.planning/phases/09-restore-clobbered-features/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and test verification with fix-up if needed</name>
  <files></files>
  <read_first>
    - .planning/phases/09-restore-clobbered-features/09-01-SUMMARY.md
    - .planning/phases/09-restore-clobbered-features/09-02-SUMMARY.md
    - .planning/phases/09-restore-clobbered-features/09-03-SUMMARY.md
    - .planning/phases/09-restore-clobbered-features/09-04-SUMMARY.md
  </read_first>
  <action>
Run full verification suite:

1. `npm run build` — must exit 0 with no TypeScript errors.
2. `npm test` — must pass ALL tests. Expected: 22+ test files (14 existing + 8 restored), ~2500+ total tests.
3. Verify all 10 previously deleted source files exist:
   ```bash
   ls src/sweep/index.ts src/batch/index.ts src/folders/cache.ts src/folders/index.ts src/log/migrations.ts src/web/frontend/folder-picker.ts src/web/routes/batch.ts src/web/routes/folders.ts src/web/routes/review-config.ts src/web/routes/review.ts
   ```
4. Verify all 8 previously deleted test files exist:
   ```bash
   ls test/integration/sweep.test.ts test/unit/batch/engine.test.ts test/unit/folders/cache.test.ts test/unit/log/migrations.test.ts test/unit/sweep/sweep.test.ts test/unit/web/batch.test.ts test/unit/web/folder-picker.test.ts test/unit/web/folders.test.ts
   ```
5. Spot-check key restorations via grep:
   - `grep 'classifyVisibility' src/imap/messages.ts` — function exists
   - `grep 'getHeaderFields' src/imap/client.ts` — method exists
   - `grep 'getReviewConfig' src/config/repository.ts` — method exists
   - `grep 'ReviewSweeper' src/index.ts` — wired in main
   - `grep 'BatchEngine' src/index.ts` — wired in main
   - `grep 'registerBatchRoutes' src/web/server.ts` — route registered
   - `grep 'envelopeHeader' src/monitor/index.ts` — envelope support
   - `grep 'EnvelopeStatus' src/shared/types.ts` — Phase 8 type preserved
   - `grep 'registerEnvelopeRoutes' src/web/server.ts` — Phase 8 route preserved
   - `grep 'esc(' src/web/frontend/app.ts` — Phase 8 XSS helper preserved
   - `grep 'batch' src/web/frontend/index.html` — batch nav exists

If any build errors or test failures:
- Read the error output carefully
- Fix the specific issue (likely a type mismatch, missing import, or mock adaptation)
- Re-run build + tests
- Repeat until green
  </action>
  <verify>
    <automated>npm run build && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npm test exits 0 with 22+ test files passing
    - All 10 deleted source files exist (ls exits 0)
    - All 8 deleted test files exist (ls exits 0)
    - grep confirmations for all 11 spot-checks return matches
  </acceptance_criteria>
  <done>Full build green, full test suite green, all 29 files verified present and functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of restored UI features</name>
  <files></files>
  <action>
Present the user with verification instructions for visual spot-checking the restored UI. No automated action — this is a human checkpoint to confirm the restored features render correctly in the browser.
  </action>
  <what-built>Restored all v0.3 features destroyed by the Phase 7 clobber: sweep engine, batch engine, folder cache, 4 route handlers, full server wiring, main entry lifecycle, frontend batch page, folder picker, review status, sweep settings, activity badges. All merged with Phase 8 additions (extended rule editor, discovery settings, XSS protection).</what-built>
  <how-to-verify>
1. Start the app: `npm start` (or `node dist/index.js` after build)
2. Open the web UI in a browser
3. Verify the nav bar has: Rules, Activity, Batch, Settings (or equivalent)
4. Click "Batch" — verify the batch filing page renders with a folder selector and dry-run button
5. Check the dashboard/home — verify review status card shows review folder info
6. Go to Settings — verify sweep settings card is visible
7. Open the rule editor (create/edit a rule) — verify:
   a. Folder picker appears for destination selection (or folder input exists)
   b. deliveredTo, visibility, readStatus fields are present (Phase 8)
8. Check Activity page — verify entries show source badges (arrival/sweep/batch)
9. Go to Settings — verify IMAP discovery section is visible (Phase 8)
  </how-to-verify>
  <verify>User confirms all UI features are visible and functional</verify>
  <acceptance_criteria>
    - User confirms batch page renders
    - User confirms rule editor has Phase 8 fields and folder picker
    - User confirms review status and sweep settings cards visible
  </acceptance_criteria>
  <done>User has visually verified all restored and preserved UI features.</done>
  <resume-signal>Type "approved" or describe any issues you see</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan — no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No new threats — verification only |
</threat_model>

<verification>
- `npm run build` green
- `npm test` green with 22+ test files
- All 18 deleted files (10 source + 8 test) confirmed present
- Visual spot-check of UI confirms batch, review, sweep, folder picker, and Phase 8 features
</verification>

<success_criteria>
Phase 9 complete: all clobbered features restored, all Phase 8 features preserved, full build and test suite green, UI visually confirmed functional.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-clobbered-features/09-05-SUMMARY.md`
</output>
