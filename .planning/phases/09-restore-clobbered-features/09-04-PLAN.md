---
phase: 09-restore-clobbered-features
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/web/frontend/api.ts
  - src/web/frontend/folder-picker.ts
  - src/web/frontend/index.html
  - src/web/frontend/styles.css
  - src/web/frontend/app.ts
  - test/unit/web/folder-picker.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Frontend API has review, folders, batch namespaces alongside Phase 8 envelope methods"
    - "Folder picker component renders a tree with expand/collapse"
    - "Batch page is accessible from the nav and renders the batch filing UI"
    - "Review status card shows sweep info on the dashboard"
    - "Sweep settings card is visible in settings page"
    - "Phase 8 rule editor with deliveredTo/visibility/readStatus is preserved"
    - "Phase 8 esc() XSS helper is preserved"
    - "Phase 8 discovery settings section is preserved"
    - "Activity badges show source type"
    - "Batch nav button exists in index.html"
  artifacts:
    - path: "src/web/frontend/api.ts"
      provides: "Full frontend API client with review/batch/folder/envelope methods"
      contains: "batch"
    - path: "src/web/frontend/folder-picker.ts"
      provides: "Tree-based folder picker component"
      min_lines: 180
    - path: "src/web/frontend/app.ts"
      provides: "Complete SPA with batch page, sweep settings, folder picker, and Phase 8 rule editor"
      contains: "renderBatchPage"
    - path: "src/web/frontend/styles.css"
      provides: "All styles including batch, sweep, folder picker, and Phase 8 discovery"
      contains: "batch"
    - path: "src/web/frontend/index.html"
      provides: "Nav with batch button"
      contains: "batch"
  key_links:
    - from: "src/web/frontend/app.ts"
      to: "src/web/frontend/api.ts"
      via: "api.batch, api.review, api.folders calls"
      pattern: "api\\.batch"
    - from: "src/web/frontend/app.ts"
      to: "src/web/frontend/folder-picker.ts"
      via: "FolderPicker import and usage"
      pattern: "FolderPicker"
---

<objective>
Restore the frontend: API client methods for review/batch/folders, folder picker component, batch nav button, batch/sweep/folder CSS, and merge the app.ts to include batch page, review status card, sweep settings, and activity badges alongside all Phase 8 additions.

Purpose: The backend is restored in Plans 01-03. This plan makes all restored features accessible through the web UI.

Output: Complete frontend with all v0.3 features restored and all Phase 8 features preserved. Build passes, folder-picker test passes.
</objective>

<execution_context>
@/Users/mike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-restore-clobbered-features/09-RESEARCH.md
@.planning/phases/09-restore-clobbered-features/09-01-SUMMARY.md

<interfaces>
From src/shared/types.ts (restored by Plan 01):
```typescript
export interface FolderNode { path, name, delimiter, flags, specialUse?, disabled?, children }
export interface FolderTreeResponse { folders, cachedAt, stale }
export interface DryRunMessage { uid, from, subject, date, ruleName }
export interface DryRunGroup { destination, action, count, messages }
export type BatchStatus = 'idle' | 'dry-running' | ...
export interface BatchStatusResponse { status, sourceFolder, ... }
export interface DryRunResponse { results }
export interface EnvelopeStatus { envelopeHeader }
export interface ReviewStatusResponse { folder, totalMessages, ... }
```

From src/web/frontend/rule-display.ts (Phase 8 addition):
```typescript
export function generateBehaviorDescription(match: EmailMatch): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore frontend API client and folder picker component</name>
  <files>src/web/frontend/api.ts, src/web/frontend/folder-picker.ts, test/unit/web/folder-picker.test.ts</files>
  <read_first>
    - src/web/frontend/api.ts (current HEAD — 46 lines, has config.getEnvelopeStatus, config.triggerDiscovery, ImapConfigResponse type)
    - git show f453be7^:src/web/frontend/api.ts (pre-clobber — has review, folders, batch, config namespaces, cursor methods, recentFolders)
    - git show f453be7^:src/web/frontend/folder-picker.ts (pre-clobber — ~198 lines, entire component)
    - git show f453be7^:test/unit/web/folder-picker.test.ts (pre-clobber — 214 lines)
  </read_first>
  <action>
**src/web/frontend/api.ts** — MERGE: Start from current HEAD (preserves Phase 8's `ImapConfigResponse`, `config.getEnvelopeStatus`, `config.triggerDiscovery`), add back pre-clobber API methods:

1. Add type imports at top for types used by restored methods. The pre-clobber imported types from `../../shared/types.js`. Add needed types: `ReviewStatusResponse`, `BatchStatusResponse`, `DryRunResponse`, `FolderTreeResponse`, `ReviewConfig`, `SweepConfig` (if used).

2. Add `review` namespace (object with methods) after existing `config` namespace:
```typescript
review: {
  status: () => request<ReviewStatusResponse>('/api/review/status'),
},
```

3. Add `folders` namespace:
```typescript
folders: {
  tree: (refresh = false) => request<FolderTreeResponse>(`/api/folders${refresh ? '?refresh=true' : ''}`),
},
```

4. Add `batch` namespace with methods: `dryRun(folder)`, `execute(folder)`, `cancel()`, `status()`. These call `/api/batch/dry-run`, `/api/batch/execute`, `/api/batch/cancel`, `/api/batch/status` respectively.

5. Add `activity.recentFolders` method:
```typescript
recentFolders: (limit = 5) => request<string[]>(`/api/activity/recent-folders?limit=${limit}`),
```

6. Add cursor config methods (if they were in pre-clobber):
```typescript
config: {
  // ... existing getEnvelopeStatus, triggerDiscovery ...
  getCursor: () => request<{ enabled: boolean }>('/api/settings/cursor'),
  setCursor: (enabled: boolean) => request<void>('/api/settings/cursor', { method: 'PUT', body: JSON.stringify({ enabled }) }),
}
```
Note: The cursor route may not have a backend handler yet. Include the client methods for completeness — they'll fail gracefully if called before a cursor route exists.

7. Add type re-exports at the bottom that pre-clobber had, if any were stripped.

Keep Phase 8's `ImapConfigResponse` type export and `config.getEnvelopeStatus`/`config.triggerDiscovery` methods exactly as they are.

**src/web/frontend/folder-picker.ts** — Restore ENTIRE file from `git show f453be7^:src/web/frontend/folder-picker.ts`. Fully deleted, no Phase 8 conflict.

**test/unit/web/folder-picker.test.ts** — Restore from `git show f453be7^:test/unit/web/folder-picker.test.ts`. Create directory `test/unit/web/` if needed (likely exists from prior plan).
  </action>
  <verify>
    <automated>npm run build && npx vitest run test/unit/web/folder-picker.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/web/frontend/api.ts contains `review:` or `review.status`
    - src/web/frontend/api.ts contains `batch:` or `batch.dryRun`
    - src/web/frontend/api.ts contains `folders:` or `folders.tree`
    - src/web/frontend/api.ts contains `getEnvelopeStatus` (Phase 8 preserved)
    - src/web/frontend/api.ts contains `triggerDiscovery` (Phase 8 preserved)
    - src/web/frontend/api.ts contains `recentFolders`
    - src/web/frontend/folder-picker.ts exists and contains `FolderPicker` or `createFolderPicker`
    - src/web/frontend/folder-picker.ts line count >= 180
    - test/unit/web/folder-picker.test.ts exists and line count >= 200
    - npm run build exits 0
    - folder-picker test passes
  </acceptance_criteria>
  <done>Frontend API has all namespaces (review, batch, folders, config with envelope, activity with recentFolders). Folder picker component restored. Test passing.</done>
</task>

<task type="auto">
  <name>Task 2: Merge app.ts with restored features and update styles/HTML</name>
  <files>src/web/frontend/app.ts, src/web/frontend/styles.css, src/web/frontend/index.html</files>
  <read_first>
    - src/web/frontend/app.ts (current HEAD — 436 lines, has Phase 8 rule editor, esc(), discovery settings, 3-column rule table)
    - git show f453be7^:src/web/frontend/app.ts (pre-clobber — has batch page, review status card, sweep settings card, activity badges, folder picker integration)
    - src/web/frontend/styles.css (current HEAD — 300 lines, has Phase 8 discovery/settings styles)
    - git show f453be7^:src/web/frontend/styles.css (pre-clobber — had batch UI, sweep badge, folder picker styles)
    - src/web/frontend/index.html (current HEAD — 21 lines, missing batch nav)
    - git show f453be7^:src/web/frontend/index.html (pre-clobber — had batch nav button)
    - src/web/frontend/rule-display.ts (current HEAD — generateBehaviorDescription, to understand Phase 8 integration)
  </read_first>
  <action>
**src/web/frontend/app.ts** — MOST COMPLEX MERGE. Start from current HEAD (preserves ALL Phase 8 additions), then ADD back the missing v0.3 features. DO NOT replace the file.

Phase 8 features to KEEP (do not touch):
- `esc()` function (XSS helper)
- Rule editor modal with deliveredTo, visibility, readStatus fields
- 3-column rule table format
- `generateBehaviorDescription(match)` usage from rule-display.ts
- Envelope status check for enabling/disabling fields
- Discovery settings section (settings page)
- Any imports from `./rule-display.js`

Pre-clobber features to ADD BACK:
1. **Import the folder picker**: `import { createFolderPicker } from './folder-picker.js';` (or however it was imported).
2. **Batch page** (`renderBatchPage` function): Renders the batch filing UI with source folder selector, dry-run button, results table, execute/cancel buttons. Uses `api.batch.dryRun()`, `api.batch.execute()`, `api.batch.cancel()`, `api.batch.status()`. Integrate with folder picker for folder selection.
3. **Review status card**: Shows review folder stats (total, unread, read messages), next sweep time, last sweep info. Uses `api.review.status()`. Add to dashboard/home page.
4. **Sweep settings card**: Shows sweep configuration (retention, schedule). Uses `api.config.getReviewConfig()` and `api.config.updateReviewConfig()` (or equivalent). Add to settings page.
5. **Activity badges**: Color-coded badges showing source type (arrival, sweep, batch) on activity entries. Add CSS classes and badge rendering in activity display.
6. **Folder picker integration**: In the rule editor modal, integrate the folder picker for selecting move destination folders instead of plain text input.
7. **Batch navigation**: Add routing for the batch page. When user clicks batch nav link, show the batch page.

Read both versions carefully. For each missing function/section in pre-clobber, find the right place to insert it in current HEAD. Key insertion points:
- Batch page function: add as new function alongside existing page renderers
- Review status card: add to dashboard render function
- Sweep settings: add to settings render function
- Activity badges: modify activity rendering
- Nav routing: add 'batch' to the navigation handler
- Folder picker: integrate into rule editor's folder input

**src/web/frontend/styles.css** — ADDITIVE merge onto current HEAD:
Keep ALL existing styles (including Phase 8 discovery/settings styles). Append the missing pre-clobber styles:
1. Batch UI styles (`.batch-*` classes): form, results table, status bar, progress
2. Sweep badge styles (`.badge-*`, `.source-badge`): color-coded activity source badges
3. Folder picker styles (`.folder-picker`, `.folder-tree`, `.folder-node`, expand/collapse)
4. Review status card styles
5. Sweep settings card styles

Read pre-clobber styles.css and current HEAD, identify all CSS blocks present in pre-clobber but missing from HEAD. Append them after the existing styles.

**src/web/frontend/index.html** — ADDITIVE merge:
Add the batch nav button to the navigation. Pre-clobber had something like:
```html
<button data-page="batch">Batch</button>
```
or equivalent in the nav bar. Read pre-clobber index.html to find exact markup and add it to current HEAD in the same nav location.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/web/frontend/app.ts contains `renderBatchPage` or `batch` page rendering function
    - src/web/frontend/app.ts contains `esc(` (Phase 8 XSS helper preserved)
    - src/web/frontend/app.ts contains `generateBehaviorDescription` (Phase 8 preserved)
    - src/web/frontend/app.ts contains `deliveredTo` (Phase 8 rule editor preserved)
    - src/web/frontend/app.ts contains `folder-picker` or `FolderPicker` or `createFolderPicker` import
    - src/web/frontend/app.ts contains review status rendering (grep for `review` and `sweep` or `status`)
    - src/web/frontend/styles.css contains `batch` (restored batch styles)
    - src/web/frontend/styles.css contains `folder-picker` or `folder-tree` (restored picker styles)
    - src/web/frontend/styles.css contains `discovery` or `settings` (Phase 8 styles preserved)
    - src/web/frontend/index.html contains `batch` (nav button)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Frontend fully merged: all v0.3 UI features restored (batch page, review status, sweep settings, activity badges, folder picker) alongside all Phase 8 features (rule editor with extended fields, discovery settings, XSS protection). Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> DOM | Frontend renders user-provided data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | app.ts DOM rendering | mitigate | Phase 8's `esc()` XSS helper MUST be used for all user-provided data rendered to DOM. Verify restored batch/review/sweep rendering also uses `esc()` for dynamic content. |
</threat_model>

<verification>
- `npm run build` passes
- `npx vitest run test/unit/web/folder-picker.test.ts` passes
- index.html has batch navigation
- styles.css has batch + folder-picker + Phase 8 styles
- app.ts has both Phase 8 rule editor AND restored batch/review/sweep UI
</verification>

<success_criteria>
Complete frontend restoration merged with Phase 8 additions. Batch page, review status, sweep settings, activity badges, and folder picker are all accessible through the UI. Phase 8 rule editor, discovery settings, and XSS protection remain intact. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-clobbered-features/09-04-SUMMARY.md`
</output>
