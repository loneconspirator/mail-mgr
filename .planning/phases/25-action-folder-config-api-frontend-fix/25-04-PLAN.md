---
phase: 25-action-folder-config-api-frontend-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/routes/folders.ts
  - test/unit/web/folders-rename.test.ts
autonomous: true
gap_closure: true
requirements: [CONF-01]

must_haves:
  truths:
    - "Renaming a folder updates all config references to that folder"
    - "review.folder is updated when the Review folder is renamed"
    - "review.trashFolder is updated when the Trash folder is renamed"
    - "review.defaultArchiveFolder is updated when the archive folder is renamed"
    - "Rule action folders are updated when their target folder is renamed"
    - "Config is persisted after folder reference updates"
  artifacts:
    - path: "src/web/routes/folders.ts"
      provides: "Config reference update logic after successful IMAP rename"
      contains: "updateReviewConfig|updateRule"
    - path: "test/unit/web/folders-rename.test.ts"
      provides: "Tests for config reference propagation on rename"
      contains: "updates config"
  key_links:
    - from: "src/web/routes/folders.ts"
      to: "deps.configRepo"
      via: "getReviewConfig + updateReviewConfig + getRules + updateRule"
      pattern: "configRepo\\.(getReviewConfig|updateReviewConfig|getRules|updateRule)"
---

<objective>
Fix folder rename to propagate path changes to all config references.

Purpose: When a user renames a folder (e.g. "Review" to "Low Priority"), all config fields
that reference that folder path must be updated. Currently the IMAP rename succeeds but
config retains the old path, causing sweep settings and rule targets to point at a
non-existent folder.

Output: Updated rename endpoint with config propagation, tests proving it works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-action-folder-config-api-frontend-fix/25-01-SUMMARY.md
@src/web/routes/folders.ts
@src/config/repository.ts
@src/config/schema.ts
@test/unit/web/folders-rename.test.ts

<interfaces>
From src/config/repository.ts:
```typescript
class ConfigRepository {
  getReviewConfig(): ReviewConfig;
  async updateReviewConfig(input: Partial<ReviewConfig>): Promise<ReviewConfig>;
  getRules(): Rule[];
  updateRule(id: string, input: Omit<Rule, 'id'>): Rule | null;
  getActionFolderConfig(): ActionFolderConfig;
}
```

From src/config/schema.ts:
```typescript
// ReviewConfig fields that hold folder paths:
//   folder: string        (default "Review")
//   trashFolder: string   (default "Trash")
//   defaultArchiveFolder: string (default "MailingLists")

// Rule action types that hold folder paths:
//   { type: 'move', folder: string }
//   { type: 'review', folder?: string }
```

From src/web/server.ts:
```typescript
interface ServerDeps {
  configRepo: ConfigRepository;
  // ... other deps
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config reference propagation to rename endpoint</name>
  <files>src/web/routes/folders.ts, test/unit/web/folders-rename.test.ts</files>
  <behavior>
    - Test 1: Renaming "Review" to "LowPriority" updates review.folder from "Review" to "LowPriority"
    - Test 2: Renaming "Trash" updates review.trashFolder
    - Test 3: Renaming "MailingLists" updates review.defaultArchiveFolder
    - Test 4: Renaming a folder that matches a rule's move action folder updates that rule
    - Test 5: Renaming a folder that matches a rule's review action folder updates that rule
    - Test 6: Renaming a folder that matches NO config references still succeeds without errors
    - Test 7: Config propagation also handles child folder renames (renaming "Archive/2024" updates rules targeting "Archive/2024")
  </behavior>
  <action>
    RED: Add tests to test/unit/web/folders-rename.test.ts in a new describe block
    "config reference propagation". The mock deps need to be enhanced:
    - configRepo mock must include getReviewConfig(), updateReviewConfig(), getRules(), updateRule()
    - Use vi.fn() for each so tests can assert calls
    - Test that after a successful rename, the endpoint calls updateReviewConfig with
      updated folder paths where oldPath matched, and calls updateRule for each rule
      whose action.folder matched oldPath.

    GREEN: In src/web/routes/folders.ts, after the successful
    `await cache.renameFolder(oldPath, fullNewPath)` call (line 81), add config
    propagation logic:

    1. Get review config via deps.configRepo.getReviewConfig()
    2. Build a partial update object checking each of these fields:
       - If review.folder === oldPath, set folder: fullNewPath
       - If review.trashFolder === oldPath, set trashFolder: fullNewPath
       - If review.defaultArchiveFolder === oldPath, set defaultArchiveFolder: fullNewPath
    3. If the partial object is non-empty, call deps.configRepo.updateReviewConfig(partial)
    4. Get all rules via deps.configRepo.getRules()
    5. For each rule where rule.action.type is 'move' or 'review' AND
       rule.action.folder === oldPath, call deps.configRepo.updateRule(rule.id, {
       ...rule without id, action: { ...rule.action, folder: fullNewPath } })
       - Strip the 'id' field from the input since updateRule signature is (id, Omit<Rule,'id'>)
    6. Wrap all config updates in try/catch - log warning but do NOT fail the rename
       response (IMAP rename already succeeded, config update failure is non-fatal)

    The response should still return { success: true, newPath: fullNewPath } regardless
    of config update outcome.
  </action>
  <verify>
    <automated>cd /Users/mike/git/mail-mgr && npx vitest run test/unit/web/folders-rename.test.ts</automated>
  </verify>
  <done>
    - All existing rename tests still pass (no regressions)
    - New tests prove review config fields are updated when folder path matches
    - New tests prove rule action folders are updated when folder path matches
    - Non-matching renames still succeed without config update calls
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to API | Untrusted oldPath/newPath from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Tampering | config propagation | mitigate | Only update config fields that exactly equal oldPath (no partial/substring matching). Existing Zod validation in ConfigRepository.updateReviewConfig and updateRule validates all updates before persisting. |
| T-25-04-02 | Denial of Service | rename endpoint | accept | Config propagation adds minimal overhead (in-memory config reads + conditional writes). Non-fatal try/catch prevents config errors from breaking IMAP rename success. |
</threat_model>

<verification>
1. `npx vitest run test/unit/web/folders-rename.test.ts` -- all tests pass
2. `npx vitest run` -- full suite passes (no regressions)
</verification>

<success_criteria>
- Renaming a folder that is referenced in review config updates those config fields
- Renaming a folder that is a rule action target updates those rules
- IMAP rename success is never blocked by config update failures
- All existing rename tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-action-folder-config-api-frontend-fix/25-04-SUMMARY.md`
</output>
