---
phase: 06-extended-message-data
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor/index.ts
  - test/unit/monitor/monitor.test.ts
autonomous: true
gap_closure: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-06

must_haves:
  truths:
    - "Monitor-processed messages have envelopeRecipient and visibility fields populated when envelopeHeader is configured"
    - "Monitor-processed messages have undefined envelopeRecipient and visibility when envelopeHeader is not configured (MATCH-06)"
  artifacts:
    - path: "src/monitor/index.ts"
      provides: "Monitor stores envelopeHeader and passes it to parseMessage"
      contains: "this.envelopeHeader"
    - path: "test/unit/monitor/monitor.test.ts"
      provides: "Tests proving envelopeHeader is passed to parseMessage"
      contains: "envelopeHeader"
  key_links:
    - from: "src/monitor/index.ts"
      to: "src/imap/messages.ts"
      via: "parseMessage(raw as ImapFetchResult, this.envelopeHeader)"
      pattern: "parseMessage.*this\\.envelopeHeader"
---

<objective>
Fix the Monitor gap where parseMessage is called without the envelopeHeader argument, causing all Monitor-processed messages to have undefined envelopeRecipient and visibility fields even when the envelope header has been discovered and configured.

Purpose: Close the one verification gap blocking Phase 6 completion. Sweep and Batch paths already work correctly -- only the Monitor's direct parseMessage call is missing the argument.
Output: Monitor passes envelopeHeader to parseMessage, with tests proving the wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-extended-message-data/06-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire envelopeHeader from Config into Monitor and parseMessage call</name>
  <files>src/monitor/index.ts, test/unit/monitor/monitor.test.ts</files>
  <read_first>
    - src/monitor/index.ts (current Monitor class -- understand constructor, processNewMessages)
    - src/imap/messages.ts (parseMessage signature: parseMessage(fetched: ImapFetchResult, envelopeHeader?: string))
    - test/unit/monitor/monitor.test.ts (existing test patterns, makeConfig helper)
    - src/config/schema.ts (Config type -- confirm imap.envelopeHeader path)
  </read_first>
  <behavior>
    - Test 1: When config.imap.envelopeHeader is set (e.g., "Delivered-To"), Monitor passes it to parseMessage as the second argument
    - Test 2: When config.imap.envelopeHeader is undefined, Monitor calls parseMessage without an envelopeHeader (existing behavior, no crash)
    - Test 3: When updateConfig or constructor is called with a new Config containing envelopeHeader, the stored value updates
  </behavior>
  <action>
    1. In test/unit/monitor/monitor.test.ts:
       - Update makeConfig to optionally accept envelopeHeader: add `envelopeHeader?: string` parameter, set it on `config.imap.envelopeHeader` when provided
       - Add a describe block "envelopeHeader passthrough" with tests:
         a. Create Monitor with config where imap.envelopeHeader = 'Delivered-To'. Mock ImapClient.fetchNewMessages to return a raw message object (with headers Buffer). Spy on the parseMessage import. Call processNewMessages(). Assert parseMessage was called with (raw, 'Delivered-To').
         b. Create Monitor with config where imap.envelopeHeader is undefined. Same flow. Assert parseMessage was called with (raw, undefined) or just (raw).

    2. In src/monitor/index.ts:
       - Add private field: `private envelopeHeader: string | undefined;`
       - In constructor, after `this.rules = config.rules;` add: `this.envelopeHeader = config.imap.envelopeHeader;`
       - Change line 112 from `parseMessage(raw as ImapFetchResult)` to `parseMessage(raw as ImapFetchResult, this.envelopeHeader)`

    Note: The envelopeHeader field is NOT readonly because when IMAP config changes, src/index.ts tears down and rebuilds the Monitor entirely (new Monitor(newConfig, deps)). There is no need for an updateEnvelopeHeader method -- the existing rebuild-on-config-change pattern handles it.
  </action>
  <verify>
    <automated>npx vitest run test/unit/monitor/monitor.test.ts -t "envelopeHeader"</automated>
  </verify>
  <acceptance_criteria>
    - src/monitor/index.ts contains `private envelopeHeader: string | undefined`
    - src/monitor/index.ts contains `this.envelopeHeader = config.imap.envelopeHeader`
    - src/monitor/index.ts contains `parseMessage(raw as ImapFetchResult, this.envelopeHeader)`
    - src/monitor/index.ts does NOT contain `parseMessage(raw as ImapFetchResult)` without the second argument
    - test/unit/monitor/monitor.test.ts contains test(s) with "envelopeHeader" in the description
    - npx vitest run test/unit/monitor/monitor.test.ts exits 0
    - npx vitest run exits 0 (full suite still passes)
  </acceptance_criteria>
  <done>Monitor passes config.imap.envelopeHeader to parseMessage, verified by tests. Monitor-processed messages will have envelopeRecipient and visibility populated when the envelope header is configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMAP server -> parseMessage | Raw IMAP fetch data parsed into EmailMessage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | parseMessage header parsing | accept | parseMessage and parseHeaderLines already handle malformed/missing headers defensively (returns undefined). No new attack surface -- this change only passes an existing config value through. |
</threat_model>

<verification>
1. `npx vitest run test/unit/monitor/monitor.test.ts` -- all monitor tests pass including new envelopeHeader tests
2. `npx vitest run` -- full suite passes (no regressions)
3. `grep -n "parseMessage.*envelopeHeader" src/monitor/index.ts` -- confirms wiring exists
4. `grep -n "parseMessage(raw as ImapFetchResult)" src/monitor/index.ts` -- should show the call WITH the second argument
</verification>

<success_criteria>
Monitor-processed messages have envelopeRecipient and visibility fields populated when config.imap.envelopeHeader is set, closing the one gap from 06-VERIFICATION.md. Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-extended-message-data/06-04-SUMMARY.md`
</output>
