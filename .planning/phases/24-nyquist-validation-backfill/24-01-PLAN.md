---
phase: 24-nyquist-validation-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md
  - .planning/phases/19-action-processing-core/19-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 18 VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "Phase 19 VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "All Phase 18 task rows show green status"
    - "All Phase 19 task rows show green status"
    - "Both files have Validation Audit section with gap counts"
    - "Both files have Test File Summary table"
  artifacts:
    - path: ".planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md"
      provides: "Nyquist-compliant validation for phase 18"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/19-action-processing-core/19-VALIDATION.md"
      provides: "Nyquist-compliant validation for phase 19"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "18-VALIDATION.md task rows"
      to: "test/unit/log/activity.test.ts, test/unit/action-folders/registry.test.ts, test/unit/rules/sender-utils.test.ts"
      via: "automated command column references"
      pattern: "green"
    - from: "19-VALIDATION.md task rows"
      to: "test/unit/action-folders/processor.test.ts"
      via: "automated command column references"
      pattern: "green"
---

<objective>
Bring Phase 18 and Phase 19 VALIDATION.md files to Nyquist compliance by running all automated commands, verifying real test coverage, updating status columns, adding missing sections (Test File Summary, Validation Audit), filling any test gaps, and flipping frontmatter to compliant.

Purpose: Close Nyquist tech debt for the first two target phases (per D-08 sequential order).
Output: Two VALIDATION.md files with `nyquist_compliant: true` matching the Phase 17 gold standard format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nyquist-validation-backfill/24-CONTEXT.md
@.planning/phases/24-nyquist-validation-backfill/24-RESEARCH.md

<interfaces>
<!-- Gold standard VALIDATION.md format from Phase 17 — use as template -->

Frontmatter fields (between YAML delimiters at top of file):
  phase: NN, slug: phase-slug, status: verified, nyquist_compliant: true, wave_0_complete: true, created: YYYY-MM-DD, verified: YYYY-MM-DD

Per-Task Verification Map row format:
```
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
```
File Exists column: `✅` if test file exists, `❌ W0` if missing
Status column: `✅ green` if passing, `❌ red` if failing, `⬜ pending` if unverified

Required sections (in order): Test Infrastructure, Sampling Rate, Per-Task Verification Map, Test File Summary, Wave 0 Requirements, Manual-Only Verifications, Validation Audit, Validation Sign-Off, Approval

Test File Summary table format:
```
| File | Tests | Coverage |
```

Validation Audit section format:
```
## Validation Audit YYYY-MM-DD

| Metric | Count |
|--------|-------|
| Gaps found | N |
| Resolved | N |
| Escalated | 0 |
| Total tests | N |
| All green | ✅ |
```

Sign-off checklist (all 6 must be checked):
- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < Ns
- [x] `nyquist_compliant: true` set in frontmatter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update Phase 18 VALIDATION.md to Nyquist compliance</name>
  <files>.planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md</files>
  <read_first>
    - .planning/phases/17-configuration-folder-lifecycle/17-VALIDATION.md (gold standard template)
    - .planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md (current draft state)
    - test/unit/log/activity.test.ts (verify test descriptions exist)
    - test/unit/action-folders/registry.test.ts (verify test descriptions exist)
    - test/unit/rules/sender-utils.test.ts (verify test descriptions exist)
  </read_first>
  <action>
Process Phase 18 VALIDATION.md per D-01 through D-07:

1. **Run each automated command** from the Per-Task Verification Map and confirm >0 tests pass (not just 0 failures):
   - `npx vitest run test/unit/log/activity.test.ts -t "action-folder"` (task 18-01-01, LOG-01)
   - `npx vitest run test/unit/log/activity.test.ts -t "rule_id"` (task 18-01-02, LOG-02)
   - `npx vitest run test/unit/action-folders/registry.test.ts` (task 18-02-01, EXT-01)
   - `npx vitest run test/unit/action-folders/registry.test.ts -t "config"` (task 18-02-02, EXT-01)
   - `npx vitest run test/unit/rules/sender-utils.test.ts` (task 18-03-01)
   - `npx vitest run test/unit/rules/sender-utils.test.ts -t "findSenderRule"` (task 18-03-02)

2. **Grep for test descriptions** matching each requirement to confirm coverage is real (per D-01):
   - grep for "action-folder" in activity.test.ts
   - grep for "rule_id" in activity.test.ts
   - grep for "registry" or "EXT-01" patterns in registry.test.ts
   - grep for "findSenderRule" and "isSenderOnly" in sender-utils.test.ts

3. **Update Per-Task Verification Map** (per D-03, D-04):
   - Change all "File Exists" columns: `❌ W0` -> `✅` for tasks 18-02-01, 18-02-02, 18-03-01, 18-03-02 (files now exist); `Extend existing` -> `✅` for 18-01-01; `Partially exists` -> `✅` for 18-01-02
   - Change all "Status" columns: `⬜ pending` -> `✅ green` for all 6 tasks (after confirming pass)

4. **Add Test File Summary table** after Per-Task Verification Map (before Wave 0 Requirements):
   ```
   ## Test File Summary

   | File | Tests | Coverage |
   |------|-------|----------|
   | `test/unit/log/activity.test.ts` | {actual count} | action-folder source logging, rule_id/rule_name fields |
   | `test/unit/action-folders/registry.test.ts` | {actual count} | Registry shape, entry count, config key alignment, destinations |
   | `test/unit/rules/sender-utils.test.ts` | {actual count} | isSenderOnly predicate, findSenderRule with case-insensitive matching |
   ```

5. **Update Wave 0 Requirements** (per D-04): Check both items `[x]` since registry.test.ts and sender-utils.test.ts now exist.

6. **Add Validation Audit section** after Manual-Only Verifications (per Phase 17 template):
   ```
   ## Validation Audit 2026-04-21

   | Metric | Count |
   |--------|-------|
   | Gaps found | {0 or actual} |
   | Resolved | {same as gaps} |
   | Escalated | 0 |
   | Total tests | {sum from Test File Summary} |
   | All green | ✅ |
   ```

7. **Check all 6 sign-off items** `[x]`.

8. **Update frontmatter** (per D-05):
   - `status: verified`
   - `nyquist_compliant: true`
   - `wave_0_complete: true`
   - Add `verified: 2026-04-21`

9. **Update Approval line**: `**Approval:** verified 2026-04-21`

If any test command returns 0 tests matched (per Pitfall 3 from RESEARCH.md), investigate the actual test description and update the automated command `-t` filter to match. If any task has no corresponding test (per D-06), write a minimal test following existing patterns in the same file (per D-07).
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md && grep -c "status: verified" .planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md && grep -c "✅ green" .planning/phases/18-safety-predicates-activity-log/18-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 18-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 18-VALIDATION.md frontmatter contains `status: verified`
    - 18-VALIDATION.md frontmatter contains `wave_0_complete: true`
    - 18-VALIDATION.md frontmatter contains `verified: 2026-04-21`
    - 18-VALIDATION.md contains exactly 6 instances of `✅ green` in the Per-Task Verification Map
    - 18-VALIDATION.md contains `## Test File Summary` section
    - 18-VALIDATION.md contains `## Validation Audit 2026-04-21` section
    - 18-VALIDATION.md Wave 0 items both show `[x]`
    - 18-VALIDATION.md sign-off items all show `[x]`
    - 18-VALIDATION.md contains `**Approval:** verified 2026-04-21`
    - No `❌ W0` markers remain in the file
    - No `⬜ pending` markers remain in the file
  </acceptance_criteria>
  <done>Phase 18 VALIDATION.md matches Phase 17 gold standard format with all rows green, all sections present, frontmatter set to nyquist_compliant: true</done>
</task>

<task type="auto">
  <name>Task 2: Audit and update Phase 19 VALIDATION.md to Nyquist compliance</name>
  <files>.planning/phases/19-action-processing-core/19-VALIDATION.md</files>
  <read_first>
    - .planning/phases/17-configuration-folder-lifecycle/17-VALIDATION.md (gold standard template)
    - .planning/phases/19-action-processing-core/19-VALIDATION.md (current draft state)
    - test/unit/action-folders/processor.test.ts (verify test descriptions for all 12 tasks)
  </read_first>
  <action>
Process Phase 19 VALIDATION.md per D-01 through D-07:

1. **Run the main test file** to confirm all tests pass:
   - `npx vitest run test/unit/action-folders/processor.test.ts`
   Confirm >0 tests pass.

2. **For each of the 12 tasks, grep processor.test.ts for explicit test descriptions** matching the requirement (per D-01). Key checks:
   - 19-01-01 (PROC-05): grep for "extractSender" or "lowercase" or "bare email"
   - 19-01-02 (PROC-06): grep for "unparseable" or "invalid.*From"
   - 19-01-03 (PROC-01): grep for "VIP" in test descriptions
   - 19-01-04 (PROC-02): grep for "Block" in test descriptions
   - 19-01-05 (PROC-03): grep for "Undo VIP"
   - 19-01-06 (PROC-04): grep for "Unblock"
   - 19-01-07 (PROC-09): grep for "conflict"
   - 19-01-08 (PROC-10): grep for "multi-field" or "specific rule"
   - 19-01-09 (RULE-01): grep for "Zod" or "validation" — **if no explicit test exists, create one** per D-06/D-07
   - 19-01-10 (RULE-02): grep for "UUID" or "descriptive name" or "correct name"
   - 19-01-11 (RULE-03): grep for "order" or "append" or "end of list"
   - 19-01-12 (RULE-04): grep for "indistinguishable" or "web UI" — **if no explicit test exists, create one** per D-06/D-07

3. **For RULE-01 gap (if found)**: Add a test in processor.test.ts that explicitly validates a created rule passes the Zod schema. Follow the existing test pattern — import the rule schema from the config module, call `ruleSchema.parse(createdRule)` and assert it does not throw. Place the test in the appropriate describe block.

4. **For RULE-04 gap (if found)**: Add a test in processor.test.ts that asserts a rule created via action folder processing has the same shape/fields as a rule created via the web UI (all required fields present: id, name, sender, action, order, matchers). Place in appropriate describe block.

5. **Update Per-Task Verification Map** (per D-03, D-04):
   - Change all "File Exists" columns: `❌ W0` -> `✅` for all 12 tasks (processor.test.ts exists)
   - Change all "Status" columns: `⬜ pending` -> `✅ green` for all 12 tasks
   - For tasks where a `-t` filter would better target coverage (e.g., 19-01-09 RULE-01), update the Automated Command to include the specific `-t` filter matching the test description

6. **Add Test File Summary table**:
   ```
   ## Test File Summary

   | File | Tests | Coverage |
   |------|-------|----------|
   | `test/unit/action-folders/processor.test.ts` | {actual count after any additions} | extractSender, unparseable From, VIP/Block/Undo VIP/Unblock processing, conflict resolution, multi-field preservation, Zod validation, UUID naming, rule ordering, rule shape |
   ```

7. **Update Wave 0 Requirements**: Check both items `[x]` since processor.test.ts and all fixtures exist.

8. **Add Validation Audit section**:
   ```
   ## Validation Audit 2026-04-21

   | Metric | Count |
   |--------|-------|
   | Gaps found | {0 or 1 or 2 based on RULE-01/RULE-04 findings} |
   | Resolved | {same as gaps} |
   | Escalated | 0 |
   | Total tests | {actual count from test run} |
   | All green | ✅ |
   ```

9. **Check all 6 sign-off items** `[x]`.

10. **Update frontmatter**: `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, add `verified: 2026-04-21`.

11. **Update Approval line**: `**Approval:** verified 2026-04-21`
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/19-action-processing-core/19-VALIDATION.md && grep -c "status: verified" .planning/phases/19-action-processing-core/19-VALIDATION.md && grep -c "✅ green" .planning/phases/19-action-processing-core/19-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 19-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 19-VALIDATION.md frontmatter contains `status: verified`
    - 19-VALIDATION.md frontmatter contains `wave_0_complete: true`
    - 19-VALIDATION.md frontmatter contains `verified: 2026-04-21`
    - 19-VALIDATION.md contains exactly 12 instances of `✅ green` in the Per-Task Verification Map
    - 19-VALIDATION.md contains `## Test File Summary` section
    - 19-VALIDATION.md contains `## Validation Audit 2026-04-21` section
    - 19-VALIDATION.md Wave 0 items all show `[x]`
    - 19-VALIDATION.md sign-off items all show `[x]`
    - 19-VALIDATION.md contains `**Approval:** verified 2026-04-21`
    - No `❌ W0` markers remain in the file
    - No `⬜ pending` markers remain in the file
    - If RULE-01 or RULE-04 tests were added: `npx vitest run test/unit/action-folders/processor.test.ts` still passes with >0 tests
  </acceptance_criteria>
  <done>Phase 19 VALIDATION.md matches Phase 17 gold standard format with all 12 rows green, all sections present, any RULE-01/RULE-04 test gaps filled, frontmatter set to nyquist_compliant: true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan modifies only planning documentation and potentially adds read-only test assertions. No production code changes, no user-facing changes, no external service interaction.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T (Tampering) | VALIDATION.md status | accept | Documentation files only — no runtime impact; git history provides audit trail |
</threat_model>

<verification>
- `grep "nyquist_compliant: true" .planning/phases/18-*/18-VALIDATION.md` returns match
- `grep "nyquist_compliant: true" .planning/phases/19-*/19-VALIDATION.md` returns match
- `npx vitest run test/unit/action-folders/processor.test.ts test/unit/log/activity.test.ts test/unit/action-folders/registry.test.ts test/unit/rules/sender-utils.test.ts` — all pass
</verification>

<success_criteria>
Phase 18 and Phase 19 VALIDATION.md files are fully Nyquist compliant: frontmatter verified, all task rows green, Test File Summary present, Validation Audit section present, sign-off complete, no pending or W0 markers remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/24-nyquist-validation-backfill/24-01-SUMMARY.md`
</output>
