---
phase: 12-retroactive-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-retroactive-verification/12-VERIFICATION.md
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - MATCH-04
  - MATCH-05
  - MATCH-06
must_haves:
  truths:
    - "All 6 MATCH requirements have been audited against source code with evidence documented"
    - "Test suite passes with all matcher, evaluator, and discovery tests green"
    - "MATCH-04 single-select confirmed acceptable by user — documented as VERIFIED with rationale note"
    - "VERIFICATION.md exists with requirement-level pass/partial/fail status and code-line evidence"
  artifacts:
    - path: ".planning/phases/12-retroactive-verification/12-VERIFICATION.md"
      provides: "Formal verification report for MATCH-01 through MATCH-06"
      contains: "MATCH-01"
  key_links:
    - from: "12-VERIFICATION.md"
      to: "src/imap/discovery.ts"
      via: "MATCH-01 evidence references"
      pattern: "discovery\\.ts"
    - from: "12-VERIFICATION.md"
      to: "src/rules/matcher.ts"
      via: "MATCH-03, MATCH-04, MATCH-05 evidence references"
      pattern: "matcher\\.ts"
    - from: "12-VERIFICATION.md"
      to: "src/rules/evaluator.ts"
      via: "MATCH-06 evidence references"
      pattern: "evaluator\\.ts"
---

<objective>
Audit all code implemented in phases 6-9 against the 6 MATCH requirements, run the full test suite as evidence, and produce a formal VERIFICATION.md artifact documenting compliance status for each requirement.

Purpose: Close the verification gap left by orphaned phases 6-9 which were executed but never formally verified. The v0.4 milestone audit identified these as needing retroactive verification before the milestone can ship.

Output: `.planning/phases/12-retroactive-verification/12-VERIFICATION.md` with pass/partial/fail per requirement and line-level code evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-retroactive-verification/12-RESEARCH.md

Source files to audit:
@src/imap/discovery.ts
@src/rules/matcher.ts
@src/rules/evaluator.ts
@src/web/routes/envelope.ts
@src/index.ts
@src/config/schema.ts
@src/imap/messages.ts
@src/web/frontend/app.ts

Test files providing evidence:
@test/unit/imap/discovery.test.ts
@test/unit/rules/matcher.test.ts
@test/unit/rules/evaluator.test.ts
@test/unit/web/api.test.ts

Format reference:
@.planning/phases/11-pattern-detection/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite and audit each MATCH requirement against source code</name>
  <files>No files modified -- this is a read-only audit task producing notes for Task 2</files>
  <read_first>
    - src/imap/discovery.ts (MATCH-01: probeEnvelopeHeaders implementation)
    - src/index.ts (MATCH-02: onImapConfigChange trigger at lines 109-136, startup discovery at lines 206-218)
    - src/web/routes/envelope.ts (MATCH-02: POST /api/config/envelope/discover endpoint)
    - src/rules/matcher.ts (MATCH-03: deliveredTo glob, MATCH-04: visibility check, MATCH-05: readStatus check)
    - src/rules/evaluator.ts (MATCH-06: needsEnvelopeData + evaluateRules skip logic)
    - src/config/schema.ts (schema definitions for all match fields, especially visibilityMatchEnum at line 32)
    - src/imap/messages.ts (MATCH-04: classifyVisibility function)
    - src/web/frontend/app.ts (MATCH-02: Run Discovery button, MATCH-06: disabled fields when envelope unavailable)
    - test/unit/imap/discovery.test.ts
    - test/unit/rules/matcher.test.ts
    - test/unit/rules/evaluator.test.ts
    - test/unit/web/api.test.ts
    - .planning/phases/11-pattern-detection/11-VERIFICATION.md (format reference)
    - .planning/phases/12-retroactive-verification/12-RESEARCH.md (requirement-to-code mapping)
  </read_first>
  <action>
1. Run the full test suite to establish baseline evidence:
   ```
   npx vitest run --reporter=verbose 2>&1
   ```
   Capture the full output -- this is primary evidence for the verification report.

2. Run the targeted MATCH-requirement test files individually for granular evidence:
   ```
   npx vitest run test/unit/imap/discovery.test.ts --reporter=verbose
   npx vitest run test/unit/rules/matcher.test.ts --reporter=verbose
   npx vitest run test/unit/rules/evaluator.test.ts --reporter=verbose
   npx vitest run test/unit/web/api.test.ts --reporter=verbose
   ```

3. For each MATCH requirement, audit the source code and record findings:

   **MATCH-01** (Auto-discovers envelope recipient header):
   - Read `src/imap/discovery.ts` -- confirm probeEnvelopeHeaders() probes CANDIDATE_HEADERS (Delivered-To, X-Delivered-To, X-Original-To, X-Resolved-To, Envelope-To) on recent messages
   - Confirm it stores the found header name via configRepo (check the return value and how callers in index.ts persist it)
   - Record: function name, line numbers, CANDIDATE_HEADERS array values, MIN_CONSENSUS threshold, test count + names

   **MATCH-02** (Triggers on config change + manual UI):
   - Read `src/index.ts` lines 109-136 -- confirm onImapConfigChange calls probeEnvelopeHeaders
   - Read `src/web/routes/envelope.ts` -- confirm POST /api/config/envelope/discover endpoint exists and calls probeEnvelopeHeaders
   - Read `src/web/frontend/app.ts` -- find "Run Discovery" button and confirm it calls the POST endpoint
   - Record: trigger mechanism, endpoint path, button selector/event handler, test evidence

   **MATCH-03** (deliveredTo glob matching with +tag variants):
   - Read `src/rules/matcher.ts` -- find deliveredTo branch, confirm it uses picomatch with nocase option
   - Confirm test cases cover: exact match, glob pattern, +tag variant, case-insensitive, angle bracket stripping
   - Record: matcher code lines, picomatch options, test case names

   **MATCH-04** (Visibility matching -- single-select vs multi-select discrepancy):
   - Read `src/config/schema.ts` line 32 -- confirm visibilityMatchEnum is z.enum(['direct','cc','bcc','list']) (single value, NOT z.array)
   - Read `src/rules/matcher.ts` visibility branch -- confirm exact equality check (not array.includes)
   - Read `src/imap/messages.ts` -- find classifyVisibility and confirm it returns a single enum value
   - Read `src/web/frontend/app.ts` -- find visibility dropdown and confirm it's `<select>` not `<select multiple>`
   - Record: VERIFIED -- implementation is single-select, requirement says "multi-select" but user confirmed single-select is acceptable. Rationale: each message has exactly one visibility classification (mutually exclusive: direct/cc/bcc/list), so single-select covers the logical domain. Document this confirmation in the verification report.

   **MATCH-05** (readStatus matching):
   - Read `src/rules/matcher.ts` readStatus branch -- confirm it checks \Seen flag
   - Confirm 'any' value acts as pass-through (no filtering)
   - Record: matcher code lines, flag checked, test case names

   **MATCH-06** (Skip rules when envelope unavailable):
   - Read `src/rules/evaluator.ts` -- confirm needsEnvelopeData() checks deliveredTo and visibility (but NOT readStatus)
   - Confirm evaluateRules() skips rules when !envelopeAvailable and needsEnvelopeData returns true
   - Read `src/web/frontend/app.ts` -- find where deliveredTo/visibility fields are disabled when envelope not configured
   - Record: function lines, skip condition, UI disable mechanism, test count

4. After auditing all 6 requirements, compile the findings into structured notes with:
   - Requirement ID
   - Status: VERIFIED / PARTIAL / FAILED
   - Primary source file + line numbers
   - Test file + test count + specific test names
   - Any discrepancies from requirement wording
   - Evidence summary (1-2 sentences)

These notes feed directly into Task 2's VERIFICATION.md creation.
  </action>
  <verify>
    <automated>cd /Users/mike/git/mail-mgr && npx vitest run test/unit/imap/discovery.test.ts test/unit/rules/matcher.test.ts test/unit/rules/evaluator.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `npx vitest run test/unit/imap/discovery.test.ts` shows 8+ tests passing
    - `npx vitest run test/unit/rules/matcher.test.ts` shows tests for deliveredTo, visibility, and readStatus passing
    - `npx vitest run test/unit/rules/evaluator.test.ts` shows tests for needsEnvelopeData and skip logic passing
    - Source code for all 6 MATCH requirements has been read and line-level evidence recorded
  </acceptance_criteria>
  <done>All 6 MATCH requirements audited against source code with line-level evidence. Test suite green. MATCH-04 discrepancy documented. Structured notes ready for VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Produce formal 12-VERIFICATION.md artifact</name>
  <files>.planning/phases/12-retroactive-verification/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-pattern-detection/11-VERIFICATION.md (format reference -- follow this structure exactly)
    - .planning/phases/12-retroactive-verification/12-RESEARCH.md (requirement-to-code mapping for cross-reference)
    - .planning/REQUIREMENTS.md (exact wording of MATCH-01 through MATCH-06)
  </read_first>
  <action>
Create `.planning/phases/12-retroactive-verification/12-VERIFICATION.md` following the Phase 11 VERIFICATION.md format exactly.

**Frontmatter fields** (use YAML frontmatter at top of file):
- phase: 12-retroactive-verification
- verified: [current ISO timestamp when writing]
- status: human_needed (because UI checks cannot be automated)
- score: 6/6 (user confirmed single-select visibility is acceptable for MATCH-04)
- overrides_applied: 0
- gaps: [] (empty array)
- human_verification: array with two items:
  1. test: "Click Run Discovery button on IMAP settings page" / expected: "Network request fires to POST /api/config/envelope/discover, discovery result updates on page" / why_human: "Browser interaction with live IMAP server cannot be automated in unit tests"
  2. test: "Remove envelope config, open rule editor, verify deliveredTo and visibility fields are disabled" / expected: "Fields appear disabled with info icon indicating envelope data unavailable" / why_human: "Frontend rendering state tied to runtime config cannot be verified in unit tests"

**Body sections (following Phase 11 format):**

1. **Header:** `# Phase 12: Retroactive Verification -- Verification Report` with phase goal, verified timestamp, status, re-verification note ("No -- initial retroactive verification of phases 6-9").

2. **Goal Achievement > Observable Truths:** Table mapping each of the 5 ROADMAP Success Criteria to status + evidence. Use the exact success criteria text from ROADMAP.md Phase 12 section:
   - SC-1: Envelope header auto-discovery code verified functional (src/imap/discovery.ts probeEnvelopeHeaders)
   - SC-2: Auto-discovery triggers on IMAP config change and via manual POST endpoint
   - SC-3: matchRule() correctly evaluates deliveredTo glob, visibility multi-select, and readStatus conditions
   - SC-4: needsEnvelopeData() skip logic confirmed -- rules using unavailable envelope data are skipped gracefully
   - SC-5: VERIFICATION.md produced confirming all 6 MATCH requirements satisfied

3. **Requirement Compliance Matrix:** Table with columns: Req ID | Status | Primary Source | Line(s) | Test File | Test Count | Notes.
   Fill in from Task 1 audit results for MATCH-01 through MATCH-06.
   MATCH-04 should show VERIFIED with a note that user confirmed single-select is acceptable despite requirement wording "multi-select".

4. **Required Artifacts:** Table of all source files that must exist with verification status. Include all 8 source files from the research requirement-to-code mapping.

5. **Key Link Verification:** Table showing wiring (From | To | Via | Status | Details):
   - discovery.ts -> index.ts (probeEnvelopeHeaders called on startup + config change)
   - index.ts -> configRepo (discovered header persisted to config)
   - envelope.ts -> discovery.ts (POST endpoint calls probeEnvelopeHeaders)
   - matcher.ts -> picomatch (deliveredTo glob matching)
   - evaluator.ts -> matcher.ts (evaluateRules calls matchRule after skip check)
   - app.ts -> envelope.ts (Run Discovery button calls POST endpoint)

6. **Test Evidence:** Summary of test runs from Task 1:
   - Total tests passing (from full suite run)
   - Per-file breakdown for the 4 key test files
   - Specific test names that cover each MATCH requirement

7. **Discrepancies:** MATCH-04 documented with exact requirement wording ("multi-select field"), actual implementation behavior (single-select z.enum), rationale for deviation (visibility is mutually exclusive per message), and resolution (user confirmed single-select is acceptable — mark as VERIFIED with note).
  </action>
  <verify>
    <automated>test -f /Users/mike/git/mail-mgr/.planning/phases/12-retroactive-verification/12-VERIFICATION.md && grep -c "MATCH-0" /Users/mike/git/mail-mgr/.planning/phases/12-retroactive-verification/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-retroactive-verification/12-VERIFICATION.md` exists
    - File contains YAML frontmatter with `phase: 12-retroactive-verification` and `verified:` timestamp
    - File contains `score:` field in frontmatter
    - File contains all 6 requirement IDs: grep for "MATCH-01", "MATCH-02", "MATCH-03", "MATCH-04", "MATCH-05", "MATCH-06" each returns at least 1 match
    - File contains MATCH-04 with VERIFIED status and note about single-select confirmation
    - File contains "multi-select" and "single-select" in the MATCH-04 discrepancy section
    - File contains "Observable Truths" section header
    - File contains "Requirement Compliance Matrix" section header
    - File contains "Key Link Verification" section header
    - File contains "human_verification" in frontmatter
    - File contains reference to `discovery.ts`, `matcher.ts`, `evaluator.ts`
  </acceptance_criteria>
  <done>12-VERIFICATION.md exists with YAML frontmatter, all 6 MATCH requirements documented with VERIFIED/PARTIAL status, line-level code evidence, test evidence, MATCH-04 discrepancy documented, key links verified, human verification items listed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is pure verification -- no new code, no new endpoints, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | VERIFICATION.md | accept | Verification artifact lives in .planning/ directory which is not served by the web app. Contains no secrets -- only references to source file paths and test names. Low risk. |

No new trust boundaries introduced. All code under verification was threat-modeled during its original implementation phases (6-9).
</threat_model>

<verification>
1. `npx vitest run` exits 0 -- full test suite green (baseline evidence)
2. `npx vitest run test/unit/imap/discovery.test.ts test/unit/rules/matcher.test.ts test/unit/rules/evaluator.test.ts test/unit/web/api.test.ts` -- all MATCH-requirement tests pass
3. `12-VERIFICATION.md` exists and contains frontmatter with `verified:` timestamp and `score:` field
4. All 6 MATCH requirement IDs appear in the verification report with status annotations
5. MATCH-04 discrepancy (multi-select vs single-select) is explicitly documented
</verification>

<success_criteria>
- 12-VERIFICATION.md produced with pass/partial status for all 6 MATCH requirements
- Full test suite passes (453+ tests green)
- All 6 MATCH requirements verified as PASS with code-line evidence (MATCH-04 single-select confirmed acceptable by user)
- Human verification items listed for UI-dependent checks (Run Discovery button, disabled fields)
</success_criteria>

<output>
After completion, create `.planning/phases/12-retroactive-verification/12-01-SUMMARY.md`
</output>
