---
phase: 23-duplicate-path-audit-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/action-folders/processor.ts
  - test/unit/action-folders/processor.test.ts
autonomous: true
requirements:
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "When a duplicate rule is detected during action folder processing, an activity log entry is written with source 'action-folder'"
    - "The activity log entry for duplicate detection includes rule_id and rule_name of the existing duplicate rule"
    - "Both VIP duplicate (duplicate-skip) and Block duplicate (duplicate-delete) paths produce activity log entries"
    - "The existing logger.debug call is preserved alongside the new logActivity call"
  artifacts:
    - path: "src/action-folders/processor.ts"
      provides: "logActivity call in duplicate branch"
      contains: "duplicate-"
    - path: "test/unit/action-folders/processor.test.ts"
      provides: "Updated idempotency tests asserting activity log entry shape"
      contains: "duplicate-skip"
  key_links:
    - from: "src/action-folders/processor.ts"
      to: "this.activityLog.logActivity"
      via: "buildActionResult + logActivity in duplicate branch"
      pattern: "logActivity.*duplicate"
---

<objective>
Add a `logActivity()` call to the duplicate-rule detection branch in `processor.ts` so that every action folder operation -- including duplicate-skip scenarios -- leaves an audit trail in the activity log.

Purpose: Closes the LOG-01/LOG-02 integration gap identified in the v0.6 milestone audit. Currently the duplicate path (PROC-07) silently moves the message without logging, creating an audit hole.

Output: Updated processor.ts with ~3 new lines and updated test assertions confirming the activity log entry shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-duplicate-path-audit-logging/23-CONTEXT.md
@.planning/phases/23-duplicate-path-audit-logging/23-RESEARCH.md

@src/action-folders/processor.ts
@test/unit/action-folders/processor.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/action-folders/processor.ts (lines 109-124):
```typescript
private buildActionResult(
  message: EmailMessage,
  action: string,
  ruleId: string,
  folder: string,
): ActionResult {
  return {
    success: true,
    messageUid: message.uid,
    messageId: message.messageId,
    action,
    folder,
    rule: ruleId,
    timestamp: new Date(),
  };
}
```

From src/log/index.ts (line 87):
```typescript
logActivity(
  result: ActionResult,
  message: EmailMessage,
  rule: Rule | null,
  source: 'arrival' | 'sweep' | 'batch' | 'action-folder' = 'arrival'
): void
```

From src/action-folders/registry.ts:
```typescript
// ACTION_REGISTRY entries have ruleAction: 'skip' | 'delete'
// actionDef.ruleAction is 'skip' for VIP, 'delete' for Block
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add logActivity to duplicate branch and update tests</name>
  <files>src/action-folders/processor.ts, test/unit/action-folders/processor.test.ts</files>

  <read_first>
    src/action-folders/processor.ts
    test/unit/action-folders/processor.test.ts
    src/log/index.ts
    src/action-folders/registry.ts
  </read_first>

  <behavior>
    - Test 1 (update existing): VIP duplicate path calls logActivity with action='duplicate-skip', source='action-folder', rule_id matching 'existing-vip' (replaces "does not log activity when duplicate detected" test at line 440)
    - Test 2 (new): Block duplicate path calls logActivity with action='duplicate-delete', source='action-folder', rule_id matching 'existing-block'
    - Test 3 (update existing): Conflict+duplicate scenario (line 455) now expects logActivity called twice -- once for conflict removal (remove-delete), once for duplicate detection (duplicate-skip). Update toHaveBeenCalledTimes(1) to toHaveBeenCalledTimes(2) and verify second call args.
  </behavior>

  <action>
**Production code change (per D-01, D-02, D-03, D-04):**

In `src/action-folders/processor.ts`, inside the `if (duplicate)` block at approximately line 60-61, add two lines AFTER the existing `this.logger.debug(...)` call (keeping it per D-04):

```typescript
const dupResult = this.buildActionResult(message, `duplicate-${actionDef.ruleAction}`, duplicate.id, destination);
this.activityLog.logActivity(dupResult, message, duplicate, 'action-folder');
```

This uses the template literal `duplicate-${actionDef.ruleAction}` which produces `duplicate-skip` for VIP and `duplicate-delete` for Block (per D-01). The `duplicate` variable (from `findSenderRule` at line 59) provides the existing rule's `id` and `name` (per D-02).

Do NOT modify any other code paths. Do NOT remove the `this.logger.debug(...)` call.

**Test changes (per D-05):**

In `test/unit/action-folders/processor.test.ts`:

1. **Replace** the test at line 440 ("does not log activity when duplicate detected") with a new test: "logs activity with duplicate-skip action when VIP duplicate detected". Assert:
   - `mockActivityLog.logActivity` was called once
   - First argument: `expect.objectContaining({ success: true, messageUid: 1, action: 'duplicate-skip', rule: 'existing-vip' })`
   - Third argument: `expect.objectContaining({ id: 'existing-vip' })`
   - Fourth argument: `'action-folder'`

2. **Add new test**: "logs activity with duplicate-delete action when Block duplicate detected". Same pattern as above but with `action: 'duplicate-delete'`, rule id `'existing-block'`, using the Block test setup from line 403.

3. **Update** the conflict+duplicate test at line 455 ("removes conflict rule then detects duplicate"):
   - Change `toHaveBeenCalledTimes(1)` at line 478 to `toHaveBeenCalledTimes(2)`
   - Add assertion for second call: `expect(mockActivityLog.logActivity).toHaveBeenNthCalledWith(2, expect.objectContaining({ action: 'duplicate-skip', rule: 'existing-vip' }), msg, expect.objectContaining({ id: 'existing-vip' }), 'action-folder')`
  </action>

  <verify>
    <automated>cd /Users/mike/git/mail-mgr && npx vitest run test/unit/action-folders/processor.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - processor.ts contains the string `duplicate-${actionDef.ruleAction}` inside the `if (duplicate)` block
    - processor.ts contains `this.activityLog.logActivity(dupResult, message, duplicate, 'action-folder')` in the duplicate branch
    - processor.ts still contains `this.logger.debug` in the duplicate branch (D-04 preserved)
    - processor.test.ts contains test with string 'duplicate-skip' in a logActivity assertion
    - processor.test.ts contains test with string 'duplicate-delete' in a logActivity assertion
    - processor.test.ts does NOT contain the assertion `not.toHaveBeenCalled()` for logActivity in the idempotency describe block (old test replaced)
    - processor.test.ts conflict+duplicate test contains `toHaveBeenCalledTimes(2)`
    - `npx vitest run test/unit/action-folders/processor.test.ts` exits 0
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>

  <done>
    Duplicate-rule detection path in processor.ts writes activity log entries with source 'action-folder' and action strings 'duplicate-skip' or 'duplicate-delete', with rule_id/rule_name referencing the existing duplicate rule. All tests pass including updated idempotency assertions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This change adds a logging call to an existing code path; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | logActivity writing duplicate rule details to SQLite | accept | Activity log is local SQLite accessed only by the application owner. No PII beyond email sender addresses already logged by other paths. Same data, same store, same access control. |
| T-23-02 | Tampering | Injected sender address in log entry | accept | Sender address is extracted upstream (processor.ts line 42) and already written to logs by other paths. No new injection surface. |
</threat_model>

<verification>
1. `npx vitest run test/unit/action-folders/processor.test.ts` -- all processor tests pass
2. `npx vitest run` -- full test suite passes (no regressions)
3. `grep -n "duplicate-" src/action-folders/processor.ts` -- shows the new action strings in the duplicate branch
4. `grep -n "duplicate-skip\|duplicate-delete" test/unit/action-folders/processor.test.ts` -- shows test assertions for both action types
</verification>

<success_criteria>
- The duplicate-rule detection branch in processor.ts calls logActivity with source 'action-folder' and action 'duplicate-skip' or 'duplicate-delete'
- The activity log entry references the existing duplicate rule's id and name (LOG-02)
- The existing logger.debug call is preserved (D-04)
- All three updated/new tests pass asserting the full activity log entry shape (D-05)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/23-duplicate-path-audit-logging/23-01-SUMMARY.md`
</output>
