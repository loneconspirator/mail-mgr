---
phase: 24-nyquist-validation-backfill
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - .planning/phases/20-monitoring-startup-recovery/20-VALIDATION.md
  - .planning/phases/21-idempotency-edge-cases/21-VALIDATION.md
  - .planning/v0.6-MILESTONE-AUDIT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Phase 20 VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "Phase 21 VALIDATION.md has nyquist_compliant: true in frontmatter"
    - "All Phase 20 task rows show green status"
    - "All Phase 21 task rows show green status"
    - "Full test suite passes after all 4 phases updated"
    - "Milestone audit reflects all 4 phases as compliant"
  artifacts:
    - path: ".planning/phases/20-monitoring-startup-recovery/20-VALIDATION.md"
      provides: "Nyquist-compliant validation for phase 20"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/21-idempotency-edge-cases/21-VALIDATION.md"
      provides: "Nyquist-compliant validation for phase 21"
      contains: "nyquist_compliant: true"
    - path: ".planning/v0.6-MILESTONE-AUDIT.md"
      provides: "Updated milestone audit with full Nyquist compliance"
      contains: "compliant_phases: [17, 18, 19, 20, 21, 22]"
  key_links:
    - from: "20-VALIDATION.md task rows"
      to: "test/unit/action-folders/poller.test.ts"
      via: "automated command column references"
      pattern: "green"
    - from: "21-VALIDATION.md task rows"
      to: "test/unit/action-folders/processor.test.ts"
      via: "automated command column references"
      pattern: "green"
    - from: "v0.6-MILESTONE-AUDIT.md nyquist section"
      to: "all 4 VALIDATION.md files"
      via: "compliant_phases list"
      pattern: "compliant_phases.*18.*19.*20.*21"
---

<objective>
Bring Phase 20 and Phase 21 VALIDATION.md files to Nyquist compliance, run the full test suite to confirm no regressions (per D-09), and update the milestone audit to reflect full Nyquist compliance across phases 17-22.

Purpose: Complete the Nyquist backfill for the remaining two phases and close the milestone-level tech debt.
Output: Two more VALIDATION.md files with `nyquist_compliant: true`, passing full suite, updated milestone audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nyquist-validation-backfill/24-CONTEXT.md
@.planning/phases/24-nyquist-validation-backfill/24-RESEARCH.md
@.planning/phases/24-nyquist-validation-backfill/24-01-SUMMARY.md

<interfaces>
<!-- Gold standard VALIDATION.md format from Phase 17 — same template as Plan 01 -->

Frontmatter: `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, `verified: YYYY-MM-DD`
Per-Task rows: `✅ green` for passing, `✅` for File Exists
Required sections: Test Infrastructure, Sampling Rate, Per-Task Verification Map, Test File Summary, Wave 0 Requirements, Manual-Only Verifications, Validation Audit, Validation Sign-Off, Approval

Milestone audit frontmatter nyquist section format:
```yaml
nyquist:
  compliant_phases: [17, 18, 19, 20, 21, 22]
  partial_phases: []
  missing_phases: []
  overall: "compliant"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update Phase 20 and Phase 21 VALIDATION.md files to Nyquist compliance</name>
  <files>.planning/phases/20-monitoring-startup-recovery/20-VALIDATION.md, .planning/phases/21-idempotency-edge-cases/21-VALIDATION.md</files>
  <read_first>
    - .planning/phases/17-configuration-folder-lifecycle/17-VALIDATION.md (gold standard template)
    - .planning/phases/20-monitoring-startup-recovery/20-VALIDATION.md (current draft state)
    - .planning/phases/21-idempotency-edge-cases/21-VALIDATION.md (current draft state)
    - test/unit/action-folders/poller.test.ts (verify test descriptions for phase 20 tasks)
    - test/unit/action-folders/processor.test.ts (verify test descriptions for phase 21 tasks)
  </read_first>
  <action>
Process Phase 20 VALIDATION.md first, then Phase 21 (per D-08 sequential order):

**--- Phase 20 ---**

1. **Run automated commands** and confirm >0 tests pass:
   - `npx vitest run test/unit/action-folders/poller.test.ts`
   Confirm all ~20 tests pass.

2. **Grep poller.test.ts** for explicit test descriptions per task (per D-01):
   - 20-01-01 (MON-01): grep for "status" — expect "calls status() for all 4 action folder paths" or similar
   - 20-01-02 (MON-02): grep for "priority" — expect priority processing test
   - 20-01-03 (FOLD-03): grep for "startup" or "scanAll" or "pending" — expect startup pre-scan test
   - 20-01-04 (FOLD-02): grep for "always-empty" or "invariant" — expect always-empty invariant tests

3. **Update Per-Task Verification Map**:
   - Change all "File Exists" columns: `❌ W0` -> `✅` for all 4 tasks (poller.test.ts exists)
   - Change all "Status" columns: `⬜ pending` -> `✅ green`

4. **Add Test File Summary table**:
   ```
   ## Test File Summary

   | File | Tests | Coverage |
   |------|-------|----------|
   | `test/unit/action-folders/poller.test.ts` | {actual count} | STATUS checks for action folders, priority processing, startup pre-scan, always-empty invariant with retry and warning |
   ```

5. **Update Wave 0 Requirements**: Check both items `[x]` since poller.test.ts and fixtures exist.

6. **Add Validation Audit section**:
   ```
   ## Validation Audit 2026-04-21

   | Metric | Count |
   |--------|-------|
   | Gaps found | 0 |
   | Resolved | 0 |
   | Escalated | 0 |
   | Total tests | {actual from test run} |
   | All green | ✅ |
   ```

7. **Check all 6 sign-off items** `[x]`.

8. **Update frontmatter**: `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, add `verified: 2026-04-21`.

9. **Update Approval line**: `**Approval:** verified 2026-04-21`

**--- Phase 21 ---**

10. **Run automated command** and confirm >0 tests pass:
    - `npx vitest run test/unit/action-folders/processor.test.ts`
    Confirm tests pass (shared file with Phase 19).

11. **Grep processor.test.ts** for explicit test descriptions per task (per D-01):
    - 21-01-01 (PROC-07): grep for "idempotency" or "PROC-07" or "duplicate" — expect "idempotency (PROC-07)" describe block with ~6 tests
    - 21-01-02 (PROC-08): grep for "no match" or "PROC-08" or "undo with no" — expect "undo with no match" describe block with ~3 tests
    - 21-01-03 (PROC-07): grep for "crash recovery" or "D-07" — expect crash recovery test

12. **Update Per-Task Verification Map**: All 3 tasks already have `✅` for File Exists. Change Status from `⬜ pending` -> `✅ green`.

13. **Update Wave 0 Requirements**: Already notes existing infrastructure covers all — confirm and leave as is.

14. **Add Test File Summary table**:
    ```
    ## Test File Summary

    | File | Tests | Coverage |
    |------|-------|----------|
    | `test/unit/action-folders/processor.test.ts` | {actual count} | Idempotency check-before-create, duplicate activity logging, undo-no-match handling, crash recovery |
    ```

15. **Add Validation Audit section** (same format as Phase 20).

16. **Check all 6 sign-off items** `[x]`.

17. **Update frontmatter**: `status: verified`, `nyquist_compliant: true`, `wave_0_complete: true`, add `verified: 2026-04-21`.

18. **Update Approval line**: `**Approval:** verified 2026-04-21`
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/20-monitoring-startup-recovery/20-VALIDATION.md && grep -c "nyquist_compliant: true" .planning/phases/21-idempotency-edge-cases/21-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 20-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 20-VALIDATION.md frontmatter contains `status: verified`
    - 20-VALIDATION.md contains exactly 4 instances of `✅ green` in Per-Task Verification Map
    - 20-VALIDATION.md contains `## Test File Summary` section
    - 20-VALIDATION.md contains `## Validation Audit 2026-04-21` section
    - 20-VALIDATION.md sign-off items all show `[x]`
    - No `❌ W0` markers remain in 20-VALIDATION.md
    - No `⬜ pending` markers remain in 20-VALIDATION.md
    - 21-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 21-VALIDATION.md frontmatter contains `status: verified`
    - 21-VALIDATION.md contains exactly 3 instances of `✅ green` in Per-Task Verification Map
    - 21-VALIDATION.md contains `## Test File Summary` section
    - 21-VALIDATION.md contains `## Validation Audit 2026-04-21` section
    - 21-VALIDATION.md sign-off items all show `[x]`
    - No `⬜ pending` markers remain in 21-VALIDATION.md
  </acceptance_criteria>
  <done>Phase 20 and Phase 21 VALIDATION.md files match Phase 17 gold standard format with all rows green, all sections present, frontmatter set to nyquist_compliant: true</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and update milestone audit for Nyquist compliance</name>
  <files>.planning/v0.6-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v0.6-MILESTONE-AUDIT.md (current audit state with partial nyquist compliance)
  </read_first>
  <action>
1. **Run full test suite** (per D-09):
   - `npx vitest run`
   - Confirm ALL tests pass (expect ~585 tests, 0 failures)
   - If any test fails, investigate and fix (should only be new tests added in Plan 01 for RULE-01/RULE-04 if applicable)

2. **Update milestone audit frontmatter** — change the `nyquist` section:
   - `compliant_phases: [17, 18, 19, 20, 21, 22]` (was `[17, 22]`)
   - `partial_phases: []` (was `[18, 19, 20, 21]`)
   - `missing_phases: []` (unchanged)
   - `overall: "compliant"` (was `"partial"`)

3. **Update tech_debt items** in frontmatter — remove the "Nyquist validation incomplete" entries from phases 18, 19, 20, 21. Keep any non-Nyquist tech debt items (e.g., phase 20's "onActionFolderConfigChange uses stale startup-time config" and phase 22's "6 human verification items pending").

4. **Add a brief note** in the audit body (after the existing content or in an appropriate section) documenting that Nyquist compliance was achieved for phases 18-21 on 2026-04-21 via Phase 24 backfill.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && grep "overall.*compliant" .planning/v0.6-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - v0.6-MILESTONE-AUDIT.md frontmatter contains `compliant_phases: [17, 18, 19, 20, 21, 22]`
    - v0.6-MILESTONE-AUDIT.md frontmatter contains `partial_phases: []`
    - v0.6-MILESTONE-AUDIT.md frontmatter contains `overall: "compliant"`
    - v0.6-MILESTONE-AUDIT.md tech_debt no longer contains "Nyquist validation incomplete" for phases 18, 19, 20, or 21
    - v0.6-MILESTONE-AUDIT.md still contains non-Nyquist tech debt items (e.g., stale config edge case, human verification items)
  </acceptance_criteria>
  <done>Full test suite green, milestone audit shows full Nyquist compliance for all v0.6 phases, tech debt entries cleaned up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — documentation and test-only changes with no production code modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | T (Tampering) | Milestone audit status | accept | Documentation only — git history provides audit trail; full test suite run confirms real compliance |
</threat_model>

<verification>
- `npx vitest run` — full suite passes (per D-09)
- `grep "nyquist_compliant: true" .planning/phases/20-*/20-VALIDATION.md` — returns match
- `grep "nyquist_compliant: true" .planning/phases/21-*/21-VALIDATION.md` — returns match
- `grep "overall.*compliant" .planning/v0.6-MILESTONE-AUDIT.md` — returns "compliant" (not "partial")
</verification>

<success_criteria>
All 4 target VALIDATION.md files (phases 18-21) are Nyquist compliant. Full test suite passes with no regressions. Milestone audit reflects complete Nyquist compliance for v0.6.
</success_criteria>

<output>
After completion, create `.planning/phases/24-nyquist-validation-backfill/24-02-SUMMARY.md`
</output>
