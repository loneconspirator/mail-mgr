---
milestone: v0.8
audited: 2026-05-05T00:00:00Z
gaps_closed: 2026-05-05T21:20:00Z
status: passed
scores:
  requirements: 12/12
  phases: 2/2
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
gaps_closed:
  - finding: "34-02 PLAN/SUMMARY misstated timeout buckets for deleteMessage/createMailbox/renameFolder (LIST→WRITE) and fetchNewMessages (WRITE→BULK_FETCH)"
    resolution: "Updated 34-02-PLAN.md per-op table and 34-02-SUMMARY.md Task 2 paragraph + Files section + Decisions to match actual code (src/imap/client.ts lines 386, 392, 420, 488)"
  - finding: "Test gap: sentinelCount === rawMessages.length skip-retry branch in poller.ts:69 had no unit coverage"
    resolution: "Added test 'skips re-check entirely when every fetched message is a sentinel' to test/unit/action-folders/poller.test.ts; verifies status() called only 4× (no recheck), fetchAllMessages once, debug log fires"
tech_debt:
  - phase: 34-generalize-fm-002-harden-imapclient-against-wedged-connectio
    items:
      - "Pre-existing gap: poll() fallback path (non-IDLE servers) lacks NOOP timeout wrapper — only usable=false short-circuit. Not introduced this milestone; defer to future hardening if non-IDLE servers become a target."
      - "Wedge detection latency is up to 120s (90s idleTimeout + 30s NOOP timeout), not 90s. R5 satisfied as written; framing in SUMMARY is informational."
nyquist:
  compliant_phases: [33, 34]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# v0.8 Milestone Audit — Action Folder Safety Hardening & FM-002 Generalization

**Audited:** 2026-05-05
**Gaps closed:** 2026-05-05 (option B path — fixed before complete-milestone)
**Status:** passed (all gaps closed; one pre-existing non-v0.8 item deferred to backlog)

## Scope

| Phase | Name | Plans | VERIFICATION | Score |
|-------|------|-------|--------------|-------|
| 33 | Action Folder Safety Hardening | 2/2 | passed | 8/8 |
| 34 | Generalize FM-002 — harden ImapClient | 3/3 | passed | 6/6 |

## Requirements Coverage

v0.8 has **no REQUIREMENTS.md file** (deleted after v0.7 per commit 11b79ce). Requirements are tracked as plan-level defect/requirement IDs:

### Phase 33 (D-01..D-07)

| ID | Description | Phase | Status | Evidence |
|----|-------------|-------|--------|----------|
| D-01 | Skip fetchAllMessages on messages === 0 (sentinel missing) | 33-02 | satisfied | poller.ts:38; empty folder test passes |
| D-02 | Skip fetchAllMessages on messages === 1 (sentinel only) | 33-02 | satisfied | poller.ts:42; sentinel-only test passes |
| D-03 | Proceed with fetchAllMessages on messages > 1 | 33-02 | satisfied | poller.ts:48; 2-msg and 5-msg tests pass |
| D-05 | Post-move activity logging (not pre-move) | 33-01 | satisfied | pendingActivities pattern in processor.ts:145-162; call-order tests pass |
| D-06 | Duplicate path early return (no fall-through) | 33-01 | satisfied | processor.ts:88-112 standalone move+log+return; D-06 tests pass |
| D-07 | Diagnostic logging (sender, subject, messageId, uid) | 33-01 | satisfied | processor.ts:57-64 emit logger.info with 6 fields |

### Phase 34 (R1..R6)

| ID | Description | Phase | Status | Evidence |
|----|-------------|-------|--------|----------|
| R1 | FM-002 + MOD-0002 spec generalization | 34-03 | satisfied | Spec rewrites; validate-failure-mode FM-002 returns 0 findings |
| R2 | guardedOp on every public op + lock acquisition | 34-01,02 | satisfied | 11 guardedOp call sites; only 1 bare `!this.flow throw` (inside guardedOp itself) |
| R3 | Bound flow.connect() + initial mailboxOpen | 34-01 | satisfied | client.ts:173-174 wraps both with CONNECT_TIMEOUT_MS |
| R4 | cleanupFlow drains in-flight via flow.close() | 34-01,02 | satisfied | client.ts:778; R4 test asserts close() called AND in-flight rejected |
| R5 | idleTimeout default 90_000 (was 300_000) | 34-02 | satisfied | schema.ts:82 .default(90_000); 22+ fixtures swept |
| R6 | FM-002 it.each test matrix N×2 ≥ 30 cases | 34-02 | satisfied | 32 matrix cases + 10 supporting = 42 FM-002 tests, all green |

**Total: 12/12 requirements satisfied.**

## Cross-Phase Integration

**Status:** WIRED. All 4 traced E2E flows complete cleanly.

- **Flow 1 (sentinel-aware skip):** poller.status() → guardedOp/LIST_TIMEOUT_MS → 0/1 skip → continue. ✓
- **Flow 2 (action-folder pipeline):** fetchAll → processor → diagnostic log → pendingActivities → moveMessage (guardedOp) → flush activities. ✓
- **Flow 3 (drag-to-Actions):** 1→2 message transition fires fetch path (UC-002 acceptance test exercises this end-to-end with GreenMail). ✓
- **Flow 4 (wedge recovery):** cycleIdle (90s) → usable=false OR NOOP timeout (30s) → handleClose → cleanupFlow.close() → drain in-flight → scheduleReconnect → bounded connect (CONNECT_TIMEOUT_MS). ✓

Phase 33's poller.status() calls now traverse Phase 34's guardedOp wrapper — clean integration, no breakage.

## Gaps Closed

### Documentation inconsistencies (Finding 1, 2) — FIXED 2026-05-05

The 34-02 SUMMARY and PLAN misstated timeout buckets for 4 ops:
- `deleteMessage`, `createMailbox`, `renameFolder`: SUMMARY said LIST_TIMEOUT_MS (15s), code uses WRITE_TIMEOUT_MS (30s)
- `fetchNewMessages`: SUMMARY said WRITE_TIMEOUT_MS (30s), code uses BULK_FETCH_TIMEOUT_MS (120s)

**Fix:** Updated 34-02-PLAN.md per-op timeout table (lines 165-177), 34-02-SUMMARY.md Task 2 paragraph (line 93), Files section (line 109), and Decisions section (line 138) to match actual code.

### Test gap (Finding 4) — FIXED 2026-05-05

`poller.ts:69` `sentinelCount === rawMessages.length` skip-retry branch lacked unit coverage.

**Fix:** Added test `'skips re-check entirely when every fetched message is a sentinel'` in `test/unit/action-folders/poller.test.ts` under `scanAll - always-empty invariant`. Asserts status() called only 4× (no recheck round-trip), fetchAllMessages once, debug log fires with `sentinels: 2`. Full poller suite: 24/24 passing. tsc: clean.

## Deferred Tech Debt (Non-Blocking)

### Pre-existing gap (Finding 3)

Private `poll()` fallback method (used when IDLE unsupported) issues bare `flow.noop()` without timeout wrapper. Not introduced by v0.8. Production impact limited to non-IDLE IMAP servers (rare; Fastmail supports IDLE). Track in backlog if non-IDLE servers become a target.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant |
|-------|---------------|-----------|
| 33 | exists | true |
| 34 | exists | true |

All phases compliant.

## Verdict

**passed** — All 12 requirements satisfied, all integration flows verified, both phases Nyquist-compliant. Two doc inconsistencies fixed and one test-coverage gap closed in the same session. One pre-existing non-v0.8 item deferred to backlog. Ready to ship.
