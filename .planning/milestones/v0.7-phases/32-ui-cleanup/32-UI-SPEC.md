---
phase: 32
slug: ui-cleanup
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-22
---

# Phase 32 — UI Design Contract

> Visual and interaction contract for Phase 32: UI Cleanup. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (vanilla DOM via helper `h()`) |
| Icon library | none |
| Font | system stack: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif |

---

## Spacing Scale

Existing project uses `rem`-based spacing (not a formal token system). This phase is pure removal — no new spacing values introduced.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (0.25rem) | Icon gaps, inline padding |
| sm | 8px (0.5rem) | Compact element spacing |
| md | 16px (1rem) | Default element spacing |
| lg | 24px (1.5rem) | Section padding |
| xl | 32px (2rem) | Layout gaps |

Exceptions: none — no new UI elements in this phase.

---

## Typography

Existing project typography. No changes in this phase.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (1rem) | 400 | 1.5 |
| Label | 14px (0.875rem) | 400 | 1.5 |
| Heading | 20px (1.25rem) | 600 | 1.2 |

---

## Color

Existing project palette. No changes in this phase.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #f5f5f5 | Page background |
| Secondary (30%) | #ffffff | Cards (.card) |
| Accent (10%) | #1a1a2e | Header, nav, primary buttons |
| Destructive | #dc2626 | Error text, destructive actions |

Accent reserved for: header bar, active nav buttons, primary CTAs. No new accent usage in this phase.

---

## Copywriting Contract

This phase is a pure removal — no new copy is introduced. The following documents what is being **removed**:

| Element | Action |
|---------|--------|
| "Folder Management" card heading | REMOVE — card deleted entirely |
| Rename folder form labels | REMOVE — form deleted entirely |
| Rename warning banner text | REMOVE — `.rename-warning` deleted |
| Rename disabled hint text | REMOVE — `.rename-disabled-hint` deleted |
| Error messages from rename API | REMOVE — endpoint deleted entirely |

No new empty states, error states, or CTAs are introduced by this phase.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | none | not applicable |

No component registries in use. Vanilla CSS + TypeScript project.

---

## Phase-Specific Removal Contract

This phase is exclusively subtractive. The design contract specifies what must be absent after implementation.

### Frontend Removals

| File | Element to Remove | Verification |
|------|-------------------|--------------|
| `src/web/frontend/app.ts` | `renderFolderRenameCard()` function (~180 lines) | Function no longer exists in file |
| `src/web/frontend/app.ts` | Call site at approx. line 1006: `await renderFolderRenameCard(app)` | No reference to `renderFolderRenameCard` in file |
| `src/web/frontend/api.ts` | `api.folders.rename()` client method (line 82) | No `rename` property on `api.folders` |
| `src/web/frontend/styles.css` | `.rename-section` class (line 684) | Class absent from stylesheet |
| `src/web/frontend/styles.css` | `.rename-disabled-hint` class (line 702) | Class absent from stylesheet |
| `src/web/frontend/styles.css` | `.rename-warning` class (line 709) | Class absent from stylesheet |

### Backend Removals

| File | Element to Remove | Verification |
|------|-------------------|--------------|
| `src/web/routes/folders.ts` | `POST /api/folders/rename` route handler (line 28+) | Route no longer registered; POST returns 404 |

### Explicitly Retained (per D-03)

| File | Element | Reason |
|------|---------|--------|
| `src/folders/cache.ts` | `renameFolder()` method | IMAP primitive used by sentinel healer |
| `src/imap/client.ts` | Low-level IMAP rename | Infrastructure method |

---

## Visual Regression Check

After removal, the settings page must:
1. Render without errors (no missing function references)
2. Show all remaining settings cards in their current layout
3. Not display any orphaned headings, buttons, or form fields related to folder rename

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
