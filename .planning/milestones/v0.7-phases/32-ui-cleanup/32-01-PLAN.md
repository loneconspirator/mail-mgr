---
phase: 32-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/routes/folders.ts
  - src/web/frontend/app.ts
  - src/web/frontend/api.ts
  - src/web/frontend/styles.css
  - test/unit/web/folders-rename.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "The settings page renders without a folder rename card"
    - "POST /api/folders/rename returns 404 (route does not exist)"
    - "No rename-related CSS classes exist in the stylesheet"
    - "TypeScript compiles with zero errors (no dangling references)"
    - "All tests pass (no import errors from deleted code)"
  artifacts:
    - path: "src/web/routes/folders.ts"
      provides: "Only GET /api/folders route — no rename handler"
      contains: "registerFolderRoutes"
    - path: "src/web/frontend/app.ts"
      provides: "Settings page without rename card"
    - path: "src/web/frontend/api.ts"
      provides: "API client with folders.list only"
    - path: "src/web/frontend/styles.css"
      provides: "Stylesheet without rename-related classes"
  key_links:
    - from: "src/web/frontend/app.ts"
      to: "src/web/frontend/api.ts"
      via: "api.folders.list (rename removed)"
      pattern: "folders.*list"
---

<objective>
Remove the manual folder rename feature from the settings page and its backing API endpoint. The sentinel auto-healing system (Phase 31) makes manual folder renames unnecessary — the system now automatically detects and heals folder renames via sentinel messages.

Purpose: Clean up superseded UI/API code to reduce maintenance surface and avoid user confusion about manual vs. automatic rename handling.
Output: Four files trimmed, one test file deleted, zero new code introduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-ui-cleanup/32-CONTEXT.md
@.planning/phases/32-ui-cleanup/32-RESEARCH.md
@.planning/phases/32-ui-cleanup/32-UI-SPEC.md

@src/web/routes/folders.ts
@src/web/frontend/app.ts
@src/web/frontend/api.ts
@src/web/frontend/styles.css
@test/unit/web/folders-rename.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove folder rename API endpoint and test file</name>
  <files>src/web/routes/folders.ts, test/unit/web/folders-rename.test.ts</files>
  <read_first>
    - src/web/routes/folders.ts (see current state — identify findNode helper at lines 5-11 and POST /api/folders/rename handler at lines 28-89)
    - test/unit/web/folders-rename.test.ts (confirm this tests the rename endpoint being removed)
  </read_first>
  <action>
**Per D-01: Hard-delete the rename endpoint entirely (no deprecation).**

In `src/web/routes/folders.ts`:
1. Delete the `findNode()` helper function (lines 5-11). This is a local function only used by the rename handler — not exported, no other consumers.
2. Delete the `POST /api/folders/rename` route handler (lines 28-89), including its request body type and all logic.
3. Remove the `FolderNode` import from the import statement on line 2 (only used by findNode and the rename handler). Keep the `ServerDeps` import.
4. After removal the file should contain ONLY: the Fastify/ServerDeps imports, the `registerFolderRoutes` export function, and the `GET /api/folders` route handler (original lines 15-26).

The resulting file should look like:
```typescript
import type { FastifyInstance } from 'fastify';
import type { ServerDeps } from '../server.js';

export function registerFolderRoutes(app: FastifyInstance, deps: ServerDeps): void {
  app.get('/api/folders', async (request, reply) => {
    const query = request.query as { refresh?: string };
    const forceRefresh = query.refresh === 'true';
    try {
      const cache = deps.getFolderCache();
      await cache.getTree(forceRefresh);
      return cache.getResponse();
    } catch (err) {
      const error = err instanceof Error ? err : new Error(String(err));
      return reply.status(503).send({ error: 'Folder list unavailable - IMAP not connected' });
    }
  });
}
```

Delete the entire file `test/unit/web/folders-rename.test.ts` — it tests the removed endpoint and will fail with import errors if kept.

**Per D-03: Do NOT touch `src/folders/cache.ts` or `src/imap/client.ts` — the low-level `renameFolder()` IMAP primitive stays.**
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run test/unit/web/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/web/routes/folders.ts does NOT contain the string `findNode`
    - src/web/routes/folders.ts does NOT contain the string `/rename`
    - src/web/routes/folders.ts does NOT contain `FolderNode` in any import
    - src/web/routes/folders.ts DOES contain `registerFolderRoutes`
    - src/web/routes/folders.ts DOES contain `app.get('/api/folders'`
    - test/unit/web/folders-rename.test.ts does NOT exist (file deleted)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run test/unit/web/` exits 0
  </acceptance_criteria>
  <done>POST /api/folders/rename route is completely gone. The findNode helper is gone. The test file is deleted. TypeScript compiles clean. Remaining web tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove folder rename UI card, API client method, and CSS</name>
  <files>src/web/frontend/app.ts, src/web/frontend/api.ts, src/web/frontend/styles.css</files>
  <read_first>
    - src/web/frontend/app.ts (see renderFolderRenameCard function at lines ~1625-1809, call site at line ~1006, clearFolderCache import at line 4)
    - src/web/frontend/api.ts (see api.folders.rename method at lines 82-85)
    - src/web/frontend/styles.css (see rename CSS block at lines 684-716)
  </read_first>
  <action>
**Per UI-01, D-02: Remove all rename UI code.**

In `src/web/frontend/app.ts`:
1. On the import line that imports from `'./folder-picker.js'` (line 4), remove `clearFolderCache` from the import. Keep `renderFolderPicker`. The line should become:
   `import { renderFolderPicker } from './folder-picker.js';`
2. Delete the call site `await renderFolderRenameCard(app);` (at approximately line 1006 in the settings page rendering function).
3. Delete the entire `renderFolderRenameCard()` function and its preceding comment header (approximately lines 1625-1809 — the function is ~180 lines). This function has zero callers after step 2.

In `src/web/frontend/api.ts`:
1. Delete the `rename` method from the `api.folders` object (lines 82-85). After removal, the `folders` property should contain only `list`:
   ```typescript
   folders: {
     list: () => request<FolderTreeResponse>('/api/folders'),
   },
   ```

In `src/web/frontend/styles.css`:
1. Delete everything from line 684 to end of file (line 716). This removes five CSS rules that are ALL rename-only:
   - `.rename-section` (line 684-689) — per D-02
   - `.field-error` (line 691-695) — only used in rename card
   - `.folder-selected` (line 697-700) — zero JS references, already dead
   - `.rename-disabled-hint` (line 702-707) — per D-02
   - `.rename-warning` (line 709-716) — per D-02
2. The file should end cleanly at what was previously line 683 (the last line before the rename CSS block).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/web/frontend/app.ts does NOT contain the string `renderFolderRenameCard`
    - src/web/frontend/app.ts does NOT contain the string `clearFolderCache`
    - src/web/frontend/app.ts DOES contain `renderFolderPicker`
    - src/web/frontend/api.ts does NOT contain the string `rename`
    - src/web/frontend/api.ts DOES contain `folders:` with `list:` inside it
    - src/web/frontend/styles.css does NOT contain `.rename-section`
    - src/web/frontend/styles.css does NOT contain `.rename-disabled-hint`
    - src/web/frontend/styles.css does NOT contain `.rename-warning`
    - src/web/frontend/styles.css does NOT contain `.field-error`
    - src/web/frontend/styles.css does NOT contain `.folder-selected`
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full suite passes)
  </acceptance_criteria>
  <done>Settings page no longer renders the folder rename card. The api.folders.rename client method is gone. All five rename-related CSS classes are gone. Full test suite passes. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this phase only removes an existing endpoint.

| Boundary | Description |
|----------|-------------|
| client -> API | Rename endpoint removed, reducing attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | POST /api/folders/rename | accept | Endpoint being deleted — threat eliminated entirely |

No new threats. This phase is purely subtractive — it reduces the attack surface by removing an authenticated endpoint.
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with no errors (catches dangling references)
2. `npx vitest run` — Full test suite passes (catches broken imports, missing functions)
3. Manual: Load settings page at http://localhost:3000 — no folder rename card visible
4. Manual: POST /api/folders/rename returns 404 (route no longer registered)
</verification>

<success_criteria>
- The folder rename card is absent from the settings page
- POST /api/folders/rename returns 404
- No rename-related CSS classes exist in styles.css
- The renderFolderRenameCard function does not exist in app.ts
- The api.folders.rename client method does not exist in api.ts
- src/folders/cache.ts renameFolder() is UNTOUCHED (per D-03)
- TypeScript compiles clean, full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/32-ui-cleanup/32-01-SUMMARY.md`
</output>
