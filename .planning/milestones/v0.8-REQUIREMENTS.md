# Archived Requirements: v0.8 Action Folder Safety Hardening & FM-002 Generalization

**Archived:** 2026-05-06 (milestone shipped)
**Source:** v0.8 was tracked via plan-level defect/requirement IDs (no top-level REQUIREMENTS.md — that file was retired in v0.7).
**Audit:** PASSED — 12/12 satisfied (see `.planning/milestones/v0.8-MILESTONE-AUDIT.md`).

## Phase 33 Requirements (D-01..D-07)

| ID | Description | Phase | Status | Evidence |
|----|-------------|-------|--------|----------|
| [x] D-01 | Skip `fetchAllMessages` on `messages === 0` (sentinel missing) | 33-02 | satisfied | `src/action-folders/poller.ts:38`; empty-folder test passes |
| [x] D-02 | Skip `fetchAllMessages` on `messages === 1` (sentinel only) | 33-02 | satisfied | `poller.ts:42`; sentinel-only test passes |
| [x] D-03 | Proceed with `fetchAllMessages` on `messages > 1` | 33-02 | satisfied | `poller.ts:48`; 2-msg and 5-msg tests pass |
| [x] D-05 | Post-move activity logging (not pre-move) | 33-01 | satisfied | `pendingActivities` pattern in `processor.ts:145-162`; call-order tests pass |
| [x] D-06 | Duplicate path early return (no fall-through) | 33-01 | satisfied | `processor.ts:88-112` standalone move+log+return; D-06 tests pass |
| [x] D-07 | Diagnostic logging (sender, subject, messageId, uid) | 33-01 | satisfied | `processor.ts:57-64` emits `logger.info` with 6 fields |

## Phase 34 Requirements (R1..R6)

| ID | Description | Phase | Status | Evidence |
|----|-------------|-------|--------|----------|
| [x] R1 | FM-002 + MOD-0002 spec generalization | 34-03 | satisfied | Spec rewrites; `validate-failure-mode FM-002` returns 0 findings |
| [x] R2 | `guardedOp` on every public op + lock acquisition | 34-01, 34-02 | satisfied | 11 `guardedOp` call sites; only 1 bare `!this.flow throw` (inside `guardedOp` itself) |
| [x] R3 | Bound `flow.connect()` + initial `mailboxOpen` | 34-01 | satisfied | `client.ts:173-174` wraps both with `CONNECT_TIMEOUT_MS` |
| [x] R4 | `cleanupFlow` drains in-flight via `flow.close()` | 34-01, 34-02 | satisfied | `client.ts:778`; R4 test asserts `close()` called AND in-flight rejected |
| [x] R5 | `idleTimeout` default `90_000` (was `300_000`) | 34-02 | satisfied | `schema.ts:82` `.default(90_000)`; 22+ fixtures swept |
| [x] R6 | FM-002 `it.each` test matrix N×2 ≥ 30 cases | 34-02 | satisfied | 32 matrix cases + 10 supporting = 42 FM-002 tests, all green |

## Outcomes

- **All requirements validated.** Implementation matches contract; no requirements adjusted or dropped.
- Two doc inconsistencies and one test-coverage gap surfaced during the milestone audit and fixed in the same session (commit `d1b7c1a`).
- One pre-existing non-v0.8 item (private `poll()` NOOP wrapper for non-IDLE servers) deferred to backlog.

## Cross-References

- Audit report: `.planning/milestones/v0.8-MILESTONE-AUDIT.md`
- Roadmap archive: `.planning/milestones/v0.8-ROADMAP.md`
- Specs: `specs/failure-modes/fm-002-wedged-imap-connection-hangs-folder-load.md`, `specs/modules/mod-0002-imap-client.md`
