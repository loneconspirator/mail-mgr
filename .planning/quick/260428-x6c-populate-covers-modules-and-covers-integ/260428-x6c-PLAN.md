---
phase: 260428-x6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/architecture.md
autonomous: true
requirements:
  - architecture-covers-frontmatter
must_haves:
  truths:
    - "specs/architecture.md frontmatter declares covers-modules: [MOD-0001..MOD-0019] (all 19 modules in specs/modules/)"
    - "specs/architecture.md frontmatter declares covers-integrations: [IX-001..IX-012] (all 12 integrations in specs/integrations/)"
    - "Every IX-### claimed in covers-integrations is mentioned by ID somewhere in the architecture body (existing mention OR new one-liner added in this plan)"
    - "/validate sweep no longer emits IX-ARCHITECTURE-NOT-MENTIONED warnings for IX-009, IX-010, IX-011, IX-012"
    - "/validate sweep no longer emits MOD-ARCHITECTURE-NOT-LINKED-BACK warnings for any MOD-#### in covers-modules"
    - "The source todo file is moved from .planning/todos/pending/ to .planning/todos/done/ after validation passes"
  artifacts:
    - path: "specs/architecture.md"
      provides: "System architecture spec with populated covers-modules and covers-integrations frontmatter"
      contains: "covers-modules: [MOD-0001"
    - path: ".planning/todos/done/2026-04-29-populate-architecture-covers-integrations-frontmatter.md"
      provides: "Closed todo evidencing this work"
  key_links:
    - from: "specs/architecture.md frontmatter"
      to: "specs/modules/mod-####-*.md and specs/integrations/ix-###-*.md"
      via: "covers-modules / covers-integrations YAML arrays consumed by the validator skills"
      pattern: "covers-(modules|integrations):\\s*\\["
---

<objective>
Populate the empty `covers-modules: []` and `covers-integrations: []` arrays in `specs/architecture.md` with every MOD/IX ID that exists on disk, add body one-liners for the four integrations not yet mentioned by ID (IX-009..IX-012), and re-run `/validate` to confirm the back-link warnings clear.

Purpose: `specs/architecture.md` is the single architecture doc; the empty coverage arrays cause `/validate` to emit IX-ARCHITECTURE-NOT-MENTIONED and MOD-ARCHITECTURE-NOT-LINKED-BACK warnings on every sweep. Closing the gap is pure spec hygiene with no code impact.

Output: Updated `specs/architecture.md`, clean `/validate` report for the affected warnings, and the source todo moved from `pending/` to `done/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-04-29-populate-architecture-covers-integrations-frontmatter.md
@specs/architecture.md
@.claude/skills/validate/SKILL.md

<id_inventory>
<!-- Verified by `ls specs/modules/` and `ls specs/integrations/` at planning time. -->
<!-- These are the canonical IDs the executor MUST use. Do NOT invent or omit. -->

Modules (19, all of MOD-0001..MOD-0019):
  MOD-0001 monitor                   MOD-0011 signal-store
  MOD-0002 imap-client               MOD-0012 proposal-store
  MOD-0003 sentinel-detector         MOD-0013 conflict-checker
  MOD-0004 rule-evaluator            MOD-0014 config-repository
  MOD-0005 rule-matcher              MOD-0015 web-server
  MOD-0006 action-executor           MOD-0016 review-sweeper
  MOD-0007 activity-log              MOD-0017 action-folder-poller
  MOD-0008 move-tracker              MOD-0018 action-folder-processor
  MOD-0009 destination-resolver      MOD-0019 batch-engine
  MOD-0010 pattern-detector

Integrations (12, all of IX-001..IX-012):
  IX-001 arrival-detection-and-rule-evaluation
  IX-002 action-execution-and-activity-logging
  IX-003 user-move-detection-and-destination-resolution
  IX-004 signal-logging-and-proposal-creation
  IX-005 proposal-approval-and-rule-creation
  IX-006 review-sweep-eligibility-and-filing
  IX-007 action-folder-polling-and-dispatch
  IX-008 action-folder-rule-mutation-and-recovery
  IX-009 batch-dry-run-preview
  IX-010 batch-execute-and-cancel
  IX-011 rule-crud-and-hot-reload
  IX-012 proposal-dismissal-and-resurfacing
</id_inventory>

<body_mention_audit>
<!-- Result of grep `MOD-00[0-9]+|IX-0[0-9]+` against specs/architecture.md at planning time. -->

Already mentioned by ID in body:
  IX-001, IX-002, IX-003, IX-004, IX-005, IX-006, IX-007, IX-008

NOT mentioned by ID in body (need one-liner added):
  IX-009, IX-010, IX-011, IX-012

Modules: NO MOD-#### IDs appear in the body — modules are referenced by name only
(e.g. "Monitor", "RuleEvaluator", "BatchEngine") in the markdown tables and Mermaid graphs.

Implication for the executor:
  - If `/validate` reports MOD-ARCHITECTURE-NOT-LINKED-BACK warnings after the
    frontmatter update, the validator wants explicit MOD-#### IDs in the body. The
    fix is to add the ID in parentheses next to each module name in the section
    tables (e.g. "**Monitor** (MOD-0001) | …"). Make that edit only if `/validate`
    actually emits the warning — name-mention may already satisfy the rule.
  - If `/validate` only reports the IX warnings, leave the module tables alone.
</body_mention_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate frontmatter and add body one-liners for IX-009..IX-012</name>
  <files>specs/architecture.md</files>
  <action>
Edit `specs/architecture.md` in two places:

(a) Replace the empty frontmatter arrays. The file currently starts with three lines between the `---` delimiters: `title: Mail-Mgr System Architecture`, `covers-modules: []`, `covers-integrations: []`. Replace those last two lines so the frontmatter contains:

  > title: Mail-Mgr System Architecture
  > covers-modules: [MOD-0001, MOD-0002, MOD-0003, MOD-0004, MOD-0005, MOD-0006,
  >                  MOD-0007, MOD-0008, MOD-0009, MOD-0010, MOD-0011, MOD-0012,
  >                  MOD-0013, MOD-0014, MOD-0015, MOD-0016, MOD-0017, MOD-0018,
  >                  MOD-0019]
  > covers-integrations: [IX-001, IX-002, IX-003, IX-004, IX-005, IX-006,
  >                       IX-007, IX-008, IX-009, IX-010, IX-011, IX-012]

(strip the leading `> ` markers — they are only here to keep this PLAN.md from being misread by the YAML parser. The actual file content uses the standard `---` … `---` frontmatter boundary already present in `specs/architecture.md`; do NOT add or remove the boundary lines.) Use exactly the IDs listed in the `<id_inventory>` block — verified against `ls specs/modules/` and `ls specs/integrations/`.

(b) Add body coverage for the four IX IDs not currently mentioned by ID (IX-009, IX-010, IX-011, IX-012). The cleanest place is a new short subsection after `### UC-001.c Variant: Review sweep delayed filing` and before the horizontal rule that precedes `## Data Flow Overview`. Add a section like:

```markdown
### Other integration entry points

- **IX-009: Batch Dry-Run Preview** — Web UI requests a dry-run preview from BatchEngine; no IMAP mutations.
- **IX-010: Batch Execute & Cancel** — Web UI commits or aborts a batch run via BatchEngine; per-message cancel honoured.
- **IX-011: Rule CRUD & Hot Reload** — Web UI mutates `config.yml` via ConfigRepository; subsystems reload without process restart.
- **IX-012: Proposal Dismissal & Resurfacing** — User dismisses a proposal via Web UI; PatternDetector resurfaces after 5 new contradicting signals.
```

Each bullet's wording must include the literal `IX-###:` prefix so the validator's body-mention check succeeds.

Do NOT modify the module tables in this task. The audit shows zero MOD-#### IDs in the body, but `/validate` may already accept name-mention. Wait for the sweep result in Task 2 before deciding whether to add MOD-#### markers.
  </action>
  <verify>
    <automated>grep -E "covers-modules:\s*\[MOD-0001" specs/architecture.md && grep -E "covers-integrations:\s*\[IX-001" specs/architecture.md && grep -c "IX-009:" specs/architecture.md && grep -c "IX-010:" specs/architecture.md && grep -c "IX-011:" specs/architecture.md && grep -c "IX-012:" specs/architecture.md</automated>
  </verify>
  <done>
Frontmatter `covers-modules` lists MOD-0001..MOD-0019 (19 IDs). Frontmatter `covers-integrations` lists IX-001..IX-012 (12 IDs). Body now contains literal `IX-009:`, `IX-010:`, `IX-011:`, `IX-012:` prefixes at least once each. No other content removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run /validate, fix any residual MOD/IX back-link warnings, and close the todo</name>
  <files>specs/architecture.md, .planning/todos/pending/2026-04-29-populate-architecture-covers-integrations-frontmatter.md, .planning/todos/done/2026-04-29-populate-architecture-covers-integrations-frontmatter.md</files>
  <action>
1. Run the `/validate` skill on the integration and module slices only (the sweep that surfaced the warnings):

   - Invoke: `/validate IX` and `/validate MOD` (separate invocations, or a single `/validate` if you want the full sweep — the per-artifact agents return per-ID verdicts either way).
   - Read the final report. Look specifically for:
     - `IX-ARCHITECTURE-NOT-MENTIONED` against any IX-001..IX-012 → must be cleared.
     - `MOD-ARCHITECTURE-NOT-LINKED-BACK` against any MOD-0001..MOD-0019 → must be cleared.

2. If `IX-ARCHITECTURE-NOT-MENTIONED` still fires for any IX-### after Task 1:
   - Re-check that the bullet's `IX-###:` prefix matches the validator's regex (it likely greps for the literal ID). Adjust the body-mention wording (e.g. switch from `IX-009:` to `(IX-009)` or vice versa) until the warning clears.

3. If `MOD-ARCHITECTURE-NOT-LINKED-BACK` fires for any MOD-####:
   - Add the ID inline in the matching module-table row. Pattern: change `| **Monitor** | …` to `| **Monitor** (MOD-0001) | …`. Only edit rows for modules the validator flagged — don't bulk-rewrite if not needed.
   - Re-run `/validate MOD` and confirm the warning clears.

4. Once both warning families are clean, move the source todo to done:
   ```bash
   mv .planning/todos/pending/2026-04-29-populate-architecture-covers-integrations-frontmatter.md \
      .planning/todos/done/2026-04-29-populate-architecture-covers-integrations-frontmatter.md
   ```

5. Do NOT chase unrelated `/validate` warnings surfaced by the sweep (e.g. IX-003/IX-004 missing integration-test, the existing pending todos in STATE.md). Those are separate todos. Mention them only in the SUMMARY for awareness.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-04-29-populate-architecture-covers-integrations-frontmatter.md && test -f .planning/todos/done/2026-04-29-populate-architecture-covers-integrations-frontmatter.md</automated>
  </verify>
  <done>
`/validate IX` and `/validate MOD` both report zero `IX-ARCHITECTURE-NOT-MENTIONED` and zero `MOD-ARCHITECTURE-NOT-LINKED-BACK` findings against the architecture file. Source todo file relocated from `pending/` to `done/`. Any unrelated warnings are surfaced in the SUMMARY but not acted on.
  </done>
</task>

</tasks>

<verification>
After Task 2 completes:

1. `grep "covers-modules:" specs/architecture.md` shows all 19 MOD IDs.
2. `grep "covers-integrations:" specs/architecture.md` shows all 12 IX IDs.
3. `grep -E "IX-(009|010|011|012):" specs/architecture.md` returns at least one match per ID.
4. `/validate IX` final report: no `IX-ARCHITECTURE-NOT-MENTIONED` for any IX-001..IX-012.
5. `/validate MOD` final report: no `MOD-ARCHITECTURE-NOT-LINKED-BACK` for any MOD-0001..MOD-0019.
6. `ls .planning/todos/pending/ | grep populate-architecture-covers` returns nothing.
7. `ls .planning/todos/done/ | grep populate-architecture-covers` returns the moved todo.
</verification>

<success_criteria>
- specs/architecture.md frontmatter declares coverage of all 19 modules and all 12 integrations using the exact IDs listed in the `<id_inventory>` block.
- Every IX-### in covers-integrations has a corresponding body mention by ID (existing or added).
- `/validate` no longer flags the architecture file for missing IX/MOD back-links.
- Source todo is moved to .planning/todos/done/ as evidence the work is complete.
- No unrelated spec edits, no source-code changes, no test changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-x6c-populate-covers-modules-and-covers-integ/260428-x6c-SUMMARY.md` covering:
- Frontmatter diff (before/after IDs).
- Body additions for IX-009..IX-012 (and MOD-#### markers if Task 2 needed them).
- `/validate` outcome (counts before vs after).
- Any unrelated warnings surfaced by the sweep that should become new pending todos (do not act on them in this plan).
</output>
