---
phase: quick-260419-s3z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/frontend/api.ts
  - src/web/routes/proposed-rules.ts
  - src/web/frontend/app.ts
autonomous: true
must_haves:
  truths:
    - "Proposed rule card shows 'Approve as Review' button between Approve and Modify"
    - "Clicking 'Approve as Review' creates a rule with action type 'review' instead of 'move'"
    - "Conflict detection (exact duplicate, shadow, Save Ahead) works identically for both approve buttons"
  artifacts:
    - path: "src/web/frontend/api.ts"
      provides: "approveAsReview API method"
      contains: "approveAsReview"
    - path: "src/web/routes/proposed-rules.ts"
      provides: "asReview query param handling"
      contains: "asReview"
    - path: "src/web/frontend/app.ts"
      provides: "Approve as Review button in proposal card"
      contains: "Approve as Review"
  key_links:
    - from: "src/web/frontend/app.ts"
      to: "src/web/frontend/api.ts"
      via: "api.proposed.approveAsReview(id)"
      pattern: "api\\.proposed\\.approveAsReview"
    - from: "src/web/frontend/api.ts"
      to: "src/web/routes/proposed-rules.ts"
      via: "POST /api/proposed-rules/:id/approve?asReview=true"
      pattern: "asReview=true"
---

<objective>
Add an "Approve as Review" button to proposed rule cards that creates a review-type rule instead of a move-type rule when approving a proposal.

Purpose: Users currently can only approve proposals as move rules. This adds the option to approve as a review rule (same sender/destination, but action type "review" instead of "move").
Output: Working button on proposal cards with full conflict detection support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/web/frontend/api.ts
@src/web/routes/proposed-rules.ts
@src/web/frontend/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asReview query param to backend + API client method</name>
  <files>src/web/routes/proposed-rules.ts, src/web/frontend/api.ts</files>
  <action>
**Backend (src/web/routes/proposed-rules.ts):**

1. Update the Querystring type on the POST /api/proposed-rules/:id/approve route to include `asReview?: string` alongside the existing `insertBefore?: string`.

2. Read `asReview` from request.query (same pattern as insertBefore on line 67):
   ```
   const asReview = (request.query as { asReview?: string }).asReview === 'true';
   ```

3. In both places where `action:` is constructed (the insertBefore path around line 107 and the normal path around line 122), use a ternary on `asReview`:
   ```
   action: asReview
     ? { type: 'review' as const, folder: proposal.destinationFolder }
     : { type: 'move' as const, folder: proposal.destinationFolder },
   ```

   There are exactly 2 `action: { type: 'move'` occurrences in this file — both must be updated.

**API client (src/web/frontend/api.ts):**

4. Add two methods to the `proposed` object (after the existing `approveInsertBefore` on line 88):
   ```
   approveAsReview: (id: number) => request<Rule>(`/api/proposed-rules/${id}/approve?asReview=true`, { method: 'POST' }),
   approveAsReviewInsertBefore: (id: number, beforeRuleId: string) => request<Rule>(`/api/proposed-rules/${id}/approve?asReview=true&insertBefore=${beforeRuleId}`, { method: 'POST' }),
   ```
  </action>
  <verify>
    <automated>cd /Users/mike/git/mail-mgr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Backend approve endpoint respects asReview=true query param to create review rules. API client exposes approveAsReview and approveAsReviewInsertBefore methods. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Approve as Review" button to proposal card UI</name>
  <files>src/web/frontend/app.ts</files>
  <action>
In the `renderProposalCard` function (starting line 994), add an "Approve as Review" button. This button must replicate the full behavior of the existing "Approve Rule" button (lines 1047-1108) but call `api.proposed.approveAsReview` instead, and use `approveAsReviewInsertBefore` for the Save Ahead flow.

1. **Create the button** (insert after the approveBtn block, before modifyBtn around line 1110):
   ```
   const approveReviewBtn = h('button', { className: 'btn btn-secondary' }, 'Approve as Review');
   ```

2. **Wire the click handler** — clone the approveBtn click handler logic with these differences:
   - Call `api.proposed.approveAsReview(p.id)` instead of `api.proposed.approve(p.id)`
   - Success toast: `'Review rule created and active.'`
   - On conflict with shadow + Save Ahead: call `api.proposed.approveAsReviewInsertBefore(p.id, conflict.rule.id)` instead of `api.proposed.approveInsertBefore`
   - Save Ahead toast: `'Review rule created ahead of shadowing rule.'`
   - On exact conflict: disable approveReviewBtn too (same as approveBtn gets disabled)
   - The spinner, button disabling, fade-out, error handling patterns are identical to approveBtn

3. **Button ordering in the actions div** — The actions div appends buttons at the end of the function. Find where buttons are appended to `actions` (should be around line 1155+). Change the append order to:
   ```
   actions.append(approveBtn, approveReviewBtn, modifyBtn, dismissBtn);
   ```
   So "Approve as Review" sits between "Approve Rule" and "Modify".

4. **Exact conflict handling** — In the approveBtn's catch block where exact conflicts disable approveBtn (line 1069), also disable approveReviewBtn:
   ```
   (approveReviewBtn as HTMLButtonElement).disabled = true;
   ```

5. **Shadow conflict handling** — In the approveBtn's catch block where shadow conflicts disable approveBtn (line 1076), also disable approveReviewBtn:
   ```
   (approveReviewBtn as HTMLButtonElement).disabled = true;
   ```
   And similarly, in the approveReviewBtn's own catch block for conflicts, disable approveBtn too.

IMPORTANT: Both approve buttons should disable each other on conflict since the conflict applies regardless of action type.
  </action>
  <verify>
    <automated>cd /Users/mike/git/mail-mgr && npx tsc --noEmit 2>&1 | head -30 && grep -c 'Approve as Review' src/web/frontend/app.ts</automated>
  </verify>
  <done>"Approve as Review" button appears on every proposal card between Approve and Modify. Uses btn-secondary styling. Full conflict detection works (exact duplicate disables both approve buttons, shadow shows Save Ahead for review variant). Spinner + disable + fade-out + toast all work identically to existing approve flow.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors: `npx tsc --noEmit`
2. Start the app and navigate to the proposals section
3. Verify "Approve as Review" button appears between "Approve Rule" and "Modify" with secondary styling
4. Click "Approve as Review" — rule should be created with action type "review"
5. Test conflict flows: if a proposal triggers a shadow or exact conflict on either approve button, both approve buttons should be disabled
</verification>

<success_criteria>
- Proposal cards show "Approve as Review" button with btn-secondary styling
- Clicking it creates a rule with action: { type: 'review', folder: destinationFolder }
- Conflict detection (exact, shadow, Save Ahead) works for both approve paths
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260419-s3z-add-approve-as-review-button-to-proposed/260419-s3z-SUMMARY.md`
</output>
