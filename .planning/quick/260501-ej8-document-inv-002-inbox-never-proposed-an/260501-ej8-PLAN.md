---
phase: quick-260501-ej8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/invariants/inv-002-inbox-never-proposed-destination.md
  - specs/modules/mod-0009-destination-resolver.md
  - specs/modules/mod-0012-proposal-store.md
  - specs/integrations/ix-003-user-move-detection-and-destination-resolution.md
  - test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts
autonomous: true
requirements: [INV-002, IX-003.8]
must_haves:
  truths:
    - "INV-002 is a first-class spec invariant with its own document, mirroring INV-001 structure."
    - "MOD-0009 (DestinationResolver) declares INV-002 in its invariants-enforced frontmatter and explains the INBOX filter in Notes."
    - "MOD-0012 (ProposalStore) declares INV-002 in its invariants-enforced frontmatter and documents the defensive INBOX guard in upsertProposal."
    - "IX-003 declares INV-002 in invariants-enforced, names IX-003.8, and adds a postcondition that destinationFolder is never INBOX."
    - "IX-003 integration test contains an IX-003.8 it() block that exercises the resolver end-to-end via GreenMail and asserts INBOX is never returned."
    - "Tracking unit tests pass and the IX-003 integration test (including IX-003.8) passes."
  artifacts:
    - path: specs/invariants/inv-002-inbox-never-proposed-destination.md
      provides: "INV-002 invariant document"
      contains: "id: INV-002"
    - path: specs/modules/mod-0009-destination-resolver.md
      provides: "MOD-0009 with INV-002 in invariants-enforced and Notes bullet"
      contains: "INV-002"
    - path: specs/modules/mod-0012-proposal-store.md
      provides: "MOD-0012 with INV-002 in invariants-enforced and Notes bullet"
      contains: "INV-002"
    - path: specs/integrations/ix-003-user-move-detection-and-destination-resolution.md
      provides: "IX-003 with invariants-enforced field, IX-003.8 interaction, INBOX postcondition"
      contains: "IX-003.8"
    - path: test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts
      provides: "IX-003.8 it() block asserting destinationFolder !== INBOX (case-insensitive)"
      contains: "IX-003.8"
  key_links:
    - from: "specs/invariants/inv-002-inbox-never-proposed-destination.md"
      to: "src/tracking/destinations.ts (isInbox helper) + src/tracking/proposals.ts (upsertProposal guard)"
      via: "enforcement frontmatter entries"
      pattern: "src/tracking/destinations.ts#isInbox"
    - from: "specs/integrations/ix-003-user-move-detection-and-destination-resolution.md"
      to: "test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts"
      via: "IX-003.8 named interaction backed by it() block"
      pattern: "IX-003.8"
    - from: "test IX-003.8 it() block"
      to: "DestinationResolver.resolveFast"
      via: "direct call after seeding ActivityLog with INBOX in recent folders"
      pattern: "resolveFast"
---

<objective>
Codify the "INBOX is never a proposed destination" rule as a first-class spec invariant (INV-002), wire it into the two affected module specs (MOD-0009, MOD-0012) and the integration spec (IX-003), and add an end-to-end integration test (IX-003.8) that exercises the resolver through GreenMail to prevent regression of the production bug fixed in commits 469af5a and 0a76bd9.

Purpose: The fix has shipped on main, but the spec docs and integration coverage do not reflect it. Without this work, future refactors can silently regress the INBOX filter, and the spec/test invariants index is incomplete.
Output: One new invariant doc, three updated spec docs, one extended integration test, two atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**AUTHORITATIVE SOURCE — READ FIRST AND TREAT AS THE SPEC FOR EVERY DELIVERABLE:**
@.planning/todos/pending/2026-05-01-document-inv-002-inbox-never-proposed.md

The todo file enumerates exact frontmatter values, exact prose to write, the 7-step test recipe, and the grep-verifiable acceptance criteria. Do not paraphrase from this PLAN — read the todo and follow it line by line for content. This PLAN only orchestrates the work and locks the verification gates.

**Formatting template (mirror this structure for INV-002):**
@specs/invariants/inv-001-imap-idle-returns-to-inbox.md

**Files to update (read current contents before editing so frontmatter merges cleanly):**
@specs/modules/mod-0009-destination-resolver.md
@specs/modules/mod-0012-proposal-store.md
@specs/integrations/ix-003-user-move-detection-and-destination-resolution.md

**Test file to extend (study existing IX-003.1 through IX-003.7 it() blocks for the seeding/move/tick patterns; do not invent new harness code):**
@test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts
@test/integration/helpers.ts

**Read-only references (production code already shipped — DO NOT MODIFY):**
@src/tracking/destinations.ts
@src/tracking/proposals.ts

**Background — the production bug this work documents:**
@.planning/debug/108-moves-to-inbox-proposed-rule.md
@.planning/quick/260430-msg-fix-inbox-destination-bug-in-proposed-ru/260430-msg-SUMMARY.md

**Hard constraints:**
- NO production code changes. The fix is on main; this task is docs + test only.
- Do NOT touch src/action-folders/, src/log/, or src/tracking/destinations.ts, or src/tracking/proposals.ts.
- Do NOT add a new failure-mode (FM) doc.
- Do NOT update specs/architecture.md.
- Frontmatter `invariants-enforced` is a real grep index — keep it bracketed and sorted (e.g. `[INV-001, INV-002]`).
- Two commits, in order: docs commit first, then test commit. Do not squash.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write INV-002 invariant doc and update MOD-0009, MOD-0012, IX-003 spec frontmatter and bodies</name>
  <files>
    specs/invariants/inv-002-inbox-never-proposed-destination.md (new),
    specs/modules/mod-0009-destination-resolver.md (modify),
    specs/modules/mod-0012-proposal-store.md (modify),
    specs/integrations/ix-003-user-move-detection-and-destination-resolution.md (modify)
  </files>
  <action>
Read the authoritative todo at .planning/todos/pending/2026-05-01-document-inv-002-inbox-never-proposed.md sections "Deliverables 1-4" and follow them verbatim. In summary:

1. Create specs/invariants/inv-002-inbox-never-proposed-destination.md mirroring the INV-001 structure (frontmatter with id/title/enforcement/modules; sections Statement, Why this exists, Enforcement, Known violation modes). Frontmatter `enforcement` must list THREE entries per the todo: code-discipline `src/tracking/destinations.ts#isInbox`, integration-test `test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts`, and code-discipline `src/tracking/proposals.ts#upsertProposal` (defensive guard). Frontmatter `modules: [MOD-0007, MOD-0009, MOD-0012]`. Statement must explicitly cite case-insensitive comparison (`folder.toUpperCase() === 'INBOX'`). "Why this exists" must reference the Soma 2026-04-30 incident and `.planning/debug/108-moves-to-inbox-proposed-rule.md`. "Known violation modes" gets a single bullet for the historical pre-2026-04-30 incident.

2. Edit specs/modules/mod-0009-destination-resolver.md: change `invariants-enforced: [INV-001]` to `invariants-enforced: [INV-001, INV-002]`. Append a new Notes bullet immediately after the existing fast-pass note documenting the INBOX filter via `isInbox(folder)` (case-insensitive) on both fast-pass and deep-scan paths. Use the exact bullet text in the todo Deliverable 2.

3. Edit specs/modules/mod-0012-proposal-store.md: change `invariants-enforced: []` to `invariants-enforced: [INV-002]`. Extend the upsertProposal Interface Summary line with the early-return clause from the todo. Add a Notes bullet noting upsertProposal short-circuits on INBOX as defense-in-depth, with primary guard in MOD-0009; reference INV-002. Use the exact prose in todo Deliverable 3.

4. Edit specs/integrations/ix-003-user-move-detection-and-destination-resolution.md: add a new frontmatter line `invariants-enforced: [INV-002]` immediately after `use-cases:`. Append IX-003.8 named interaction (text per todo Deliverable 4) after the existing IX-003.7 bullet. Add the new Postconditions bullet ("A confirmed move signal's `destinationFolder` is never INBOX.").

DO NOT modify any file under src/. DO NOT add or modify failure-mode docs. DO NOT touch architecture.md.

After all four files are written, stage and commit ONLY these four files with message:
`docs(specs): add INV-002 (INBOX never proposed), update MOD-0009/MOD-0012/IX-003`
  </action>
  <verify>
    <automated>test -f specs/invariants/inv-002-inbox-never-proposed-destination.md && grep -q "^id: INV-002" specs/invariants/inv-002-inbox-never-proposed-destination.md && grep -q "INV-002" specs/modules/mod-0009-destination-resolver.md && grep -q "INV-002" specs/modules/mod-0012-proposal-store.md && grep -q "IX-003.8" specs/integrations/ix-003-user-move-detection-and-destination-resolution.md && grep -q "invariants-enforced: \[INV-002\]" specs/integrations/ix-003-user-move-detection-and-destination-resolution.md && grep -q "invariants-enforced: \[INV-001, INV-002\]" specs/modules/mod-0009-destination-resolver.md && grep -q "invariants-enforced: \[INV-002\]" specs/modules/mod-0012-proposal-store.md && git log -1 --format=%s | grep -q "^docs(specs):"</automated>
  </verify>
  <done>
- specs/invariants/inv-002-inbox-never-proposed-destination.md exists with INV-001-shaped frontmatter and the four required sections.
- mod-0009 frontmatter is `[INV-001, INV-002]`; new Notes bullet present referencing `isInbox(folder)` and INV-002.
- mod-0012 frontmatter is `[INV-002]`; upsertProposal interface line mentions early return on INBOX; Notes bullet present referencing defense-in-depth and INV-002.
- ix-003 has `invariants-enforced: [INV-002]` line, a new IX-003.8 named interaction bullet, and a new Postconditions bullet stating destinationFolder is never INBOX.
- No files under src/ are modified (verify with `git diff --name-only HEAD~1 HEAD | grep -c '^src/'` returning 0).
- One docs commit landed touching exactly the four spec files listed in `<files>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add IX-003.8 integration test asserting INBOX is never returned as destination</name>
  <files>test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts</files>
  <action>
Read the authoritative todo at .planning/todos/pending/2026-05-01-document-inv-002-inbox-never-proposed.md section "Deliverable 5" and follow the 7-step recipe verbatim.

Concretely:

1. Update the file's header comment block (which currently lists IX-003.1 through IX-003.7) to add a line for IX-003.8 with the exact wording from the todo ("IX-003.8 — DestinationResolver excludes INBOX from candidates even when ActivityLog returns INBOX in recent folders (INV-002).").

2. Inside the existing `describe('IX-003 ...')` block, after the IX-003.7 (deep-scan miss) test, add a new `it()` block titled with an "IX-003.8" prefix. The test MUST:
   a. Seed ActivityLog so `getRecentFolders()` returns INBOX plus the review folder plus at least one other (e.g. Archive). Prefer the existing `activityLog.logActivity(...)` API used elsewhere in this file. Direct INSERT into the activity table is acceptable as a fallback per the todo, but match the seeding style of neighboring tests if possible.
   b. Send a test message via `sendTestEmail` to a NON-INBOX folder (use the same REVIEW_FOLDER constant other tests in this file use).
   c. Plant the same Message-ID into INBOX as a second copy (sendTestEmail to INBOX, or an IMAP APPEND helper from helpers.ts if one exists). This reproduces the bug condition.
   d. Manually move the message OUT of the review folder, mirroring the pattern in the IX-003.4 fast-pass test.
   e. Drive MoveTracker tick(s) sufficient to produce a signal — copy the tick-driving idiom from neighboring tests in this file.
   f. Assert the emitted signal's `destinationFolder` satisfies `.toUpperCase() !== 'INBOX'`. Either a non-INBOX resolution OR a null/unresolved destination is acceptable; the only forbidden value is INBOX (any case).
   g. As a secondary direct assertion, call `destinationResolver.resolveFast(messageId, REVIEW_FOLDER)` and assert the return value is never `'INBOX'` (case-insensitive).

3. Use REAL components — do NOT mock DestinationResolver, ActivityLog, or MoveTracker. Match the GreenMail-backed real-component convention of this file.

4. Run the targeted suites and confirm both pass:
   - `npm run test:unit -- test/unit/tracking/`
   - `npm run test:integration -- test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts`
   GreenMail must be running for the integration suite — see `test/integration/global-setup.ts` and `helpers.ts:assertGreenMailRunning`. If GreenMail is not running, start it before running the integration test; do not skip the test.

5. Commit ONLY the test file with message:
   `test(IX-003.8): assert INBOX never resolved as destination`

Do NOT touch any spec file in this task — those landed in Task 1's commit. Do NOT modify any file under src/.
  </action>
  <verify>
    <automated>grep -q "IX-003.8" test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts && grep -q "toUpperCase()" test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts && npm run test:unit -- test/unit/tracking/ && npm run test:integration -- test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts && git log -1 --format=%s | grep -q "^test(IX-003.8):"</automated>
  </verify>
  <done>
- File header comment lists IX-003.8 alongside IX-003.1 through IX-003.7.
- A new it() block with "IX-003.8" in its title exists inside the existing describe() block, after the IX-003.7 test.
- The new test seeds ActivityLog so INBOX is in the recent-folders pool, plants a duplicate Message-ID in INBOX, moves the original out of the review folder, drives ticks, and asserts both (a) the signal's destinationFolder is not INBOX (case-insensitive) and (b) resolveFast directly returns non-INBOX.
- No mocks of DestinationResolver, ActivityLog, or MoveTracker are introduced.
- `npm run test:unit -- test/unit/tracking/` exits 0.
- `npm run test:integration -- test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts` exits 0.
- One test commit landed touching only the integration test file.
- `git diff --name-only HEAD~1 HEAD | grep -c '^src/'` returns 0 (no production code changed).
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the seven grep-verifiable acceptance criteria from the todo MUST all hold:

```bash
test -f specs/invariants/inv-002-inbox-never-proposed-destination.md
grep -l "INV-002" specs/modules/mod-0009-destination-resolver.md
grep -l "INV-002" specs/modules/mod-0012-proposal-store.md
grep -l "IX-003.8" specs/integrations/ix-003-user-move-detection-and-destination-resolution.md
grep -l "IX-003.8" test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts
npm run test:unit
npm run test:integration -- test/integration/ix-003-user-move-detection-and-destination-resolution.test.ts
```

Plus structural integrity checks:
- `git log --oneline HEAD~2..HEAD` shows exactly two new commits, in order: `docs(specs): ...` then `test(IX-003.8): ...`.
- `git diff --name-only HEAD~2 HEAD -- src/` returns nothing (no production code touched).
- Frontmatter `invariants-enforced` lists are bracketed and sorted: `[INV-001, INV-002]` in MOD-0009, `[INV-002]` in MOD-0012 and IX-003.
</verification>

<success_criteria>
- INV-002 exists as a first-class spec invariant document, structured to match INV-001.
- MOD-0009, MOD-0012, and IX-003 reference INV-002 in their frontmatter `invariants-enforced` index.
- IX-003 declares IX-003.8 as a named interaction and a corresponding postcondition.
- The IX-003 integration test contains a new IX-003.8 it() block that exercises the resolver end-to-end through GreenMail and asserts INBOX is never returned (case-insensitive), backed by a direct `resolveFast` assertion.
- Tracking unit tests pass; IX-003 integration test (including IX-003.8) passes.
- Two atomic commits land on the working branch in order (docs first, test second), with zero changes under src/.
</success_criteria>

<output>
After completion, create `.planning/quick/260501-ej8-document-inv-002-inbox-never-proposed-an/260501-ej8-SUMMARY.md` summarizing what was documented, what the test asserts, and the two commit SHAs. Then move the todo from `.planning/todos/pending/` to `.planning/todos/done/` with a completion-date prefix per project convention.
</output>
